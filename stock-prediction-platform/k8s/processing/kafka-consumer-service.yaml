apiVersion: v1
kind: Service
metadata:
  name: kafka-consumer
  namespace: processing
  labels:
    app: kafka-consumer
spec:
  selector:
    app: kafka-consumer
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
