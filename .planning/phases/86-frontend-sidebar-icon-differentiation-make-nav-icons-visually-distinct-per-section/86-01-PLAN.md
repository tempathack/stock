---
phase: 86-frontend-sidebar-icon-differentiation-make-nav-icons-visually-distinct-per-section
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx
autonomous: false
requirements:
  - NAV-ICON-01
must_haves:
  truths:
    - "The Models nav item uses PsychologyIcon (brain/organic blob silhouette), not AccountTreeIcon"
    - "The Drift nav item uses WaterDropIcon (droplet silhouette), not BubbleChartIcon"
    - "The Backtest nav item uses HistoryIcon (clock/time silhouette), not SsidChartIcon"
    - "The Analytics nav item uses InsightsIcon (sparkline+dot silhouette), not BarChartIcon"
    - "Dashboard and Forecasts nav items remain unchanged (DashboardIcon, TrendingUpIcon)"
    - "No two nav icons share the same visual family"
    - "Active/inactive color contract is unchanged — no sx prop changes on the Icon element"
  artifacts:
    - path: "stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx"
      provides: "TopNav with six distinct nav icons"
      contains: "PsychologyIcon"
  key_links:
    - from: "navItems array"
      to: "Icon: PsychologyIcon / WaterDropIcon / HistoryIcon / InsightsIcon"
      via: "direct assignment in navItems object literal"
      pattern: "Icon: PsychologyIcon"
---

<objective>
Replace four visually similar chart-style nav icons in TopNav with icons that have distinct silhouettes, so each nav section is identifiable by icon shape alone at 15px rendered size.

Purpose: Users scanning the navigation bar cannot differentiate sections by icon shape today — four icons share a thin-line chart motif. Replacing them gives each section a unique silhouette family.
Output: Updated Sidebar.tsx with four new icon imports and four updated navItems entries. Zero other changes.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-frontend-sidebar-icon-differentiation-make-nav-icons-visually-distinct-per-section/86-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap four nav icons in Sidebar.tsx</name>
  <files>stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx</files>

  <read_first>
    - stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx
      (read the full file — confirm current imports and navItems array before touching anything)
    - .planning/phases/86-frontend-sidebar-icon-differentiation-make-nav-icons-visually-distinct-per-section/86-UI-SPEC.md
      (icon assignment contract, import paths, fallback rules)
  </read_first>

  <action>
Make the following targeted changes to Sidebar.tsx. No other lines may change.

**Step 1 — Remove four stale icon imports (lines 8–11):**

DELETE these four lines:
  import AccountTreeIcon from "@mui/icons-material/AccountTree";
  import BarChartIcon from "@mui/icons-material/BarChart";
  import BubbleChartIcon from "@mui/icons-material/BubbleChart";
  import SsidChartIcon from "@mui/icons-material/SsidChart";

**Step 2 — Add four replacement icon imports after line 7 (the TrendingUpIcon import):**

  import PsychologyIcon from "@mui/icons-material/Psychology";
  import WaterDropIcon  from "@mui/icons-material/WaterDrop";
  import HistoryIcon    from "@mui/icons-material/History";
  import InsightsIcon   from "@mui/icons-material/Insights";

**Step 3 — Update navItems array (lines 14–21). Replace the four stale Icon references:**

Before:
  const navItems = [
    { to: "/dashboard", label: "Dashboard",  Icon: DashboardIcon  },
    { to: "/models",    label: "Models",      Icon: AccountTreeIcon },
    { to: "/forecasts", label: "Forecasts",   Icon: TrendingUpIcon  },
    { to: "/drift",     label: "Drift",       Icon: BubbleChartIcon },
    { to: "/backtest",  label: "Backtest",    Icon: SsidChartIcon   },
    { to: "/analytics", label: "Analytics",   Icon: BarChartIcon    },
  ];

After:
  const navItems = [
    { to: "/dashboard", label: "Dashboard",  Icon: DashboardIcon   },
    { to: "/models",    label: "Models",      Icon: PsychologyIcon  },
    { to: "/forecasts", label: "Forecasts",   Icon: TrendingUpIcon  },
    { to: "/drift",     label: "Drift",       Icon: WaterDropIcon   },
    { to: "/backtest",  label: "Backtest",    Icon: HistoryIcon     },
    { to: "/analytics", label: "Analytics",   Icon: InsightsIcon    },
  ];

**Fallback check:** The icon imports (Psychology, WaterDrop, History, Insights) are available in @mui/icons-material v7 per UI-SPEC.md. If a TypeScript error appears for a specific icon at build time, apply:
- PsychologyIcon unavailable: use AutoAwesomeIcon from "@mui/icons-material/AutoAwesome"
- WaterDropIcon unavailable: use WavesIcon from "@mui/icons-material/Waves"

**Do NOT change:**
- Any sx props on the Icon element (lines 231–238 in the original file)
- Typography, spacing, color values, or any other nav item properties
- The StatusDot component, clock logic, or K8s status display
  </action>

  <verify>
    <automated>cd /home/tempa/Desktop/priv-project/stock-prediction-platform/services/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "PsychologyIcon" stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx returns two hits (import line + navItems line)
    - grep -n "WaterDropIcon" stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx returns two hits (import line + navItems line)
    - grep -n "HistoryIcon" stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx returns two hits (import line + navItems line)
    - grep -n "InsightsIcon" stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx returns two hits (import line + navItems line)
    - grep "AccountTreeIcon\|BubbleChartIcon\|SsidChartIcon\|BarChartIcon" stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx returns zero results
    - npx tsc --noEmit exits with code 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>Four replacement icons imported and wired into navItems. Four stale imports removed. TypeScript compiles clean. No other lines changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification via Playwright MCP</name>
  <files>stock-prediction-platform/services/frontend/src/components/layout/Sidebar.tsx</files>
  <action>No code changes. Verify the running frontend visually using Playwright MCP as described in how-to-verify.</action>
  <verify>
    <automated>MISSING — this task is a visual checkpoint; automated verification is the Playwright MCP screenshot step in how-to-verify</automated>
  </verify>
  <what-built>Four nav icon replacements in TopNav: PsychologyIcon (Models), WaterDropIcon (Drift), HistoryIcon (Backtest), InsightsIcon (Analytics). Dashboard and Forecasts icons unchanged.</what-built>
  <how-to-verify>
    Use Playwright MCP to verify the nav bar:
    1. Navigate to the frontend (e.g., http://localhost:5173 or the running dev server URL)
    2. Take a screenshot of the top navigation bar
    3. Confirm the six icons are visually distinct — no two look like chart variations of each other
    4. Click each nav item and confirm the active state (purple glow + underline gradient) still applies to whichever icon is active
    5. Confirm no console errors related to missing icon modules

    Expected: Six distinct icon shapes visible — grid tile, brain, upward arrow, droplet, clock, sparkline+dot.
  </how-to-verify>
  <done>User approves six visually distinct icons and functioning active state via Playwright screenshot.</done>
  <resume-signal>Type "approved" if icons are visually distinct and active state works, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation passes: cd stock-prediction-platform/services/frontend and run npx tsc --noEmit
- No stale imports remain: grep "AccountTreeIcon\|BubbleChartIcon\|SsidChartIcon\|BarChartIcon" Sidebar.tsx returns zero lines
- All four new imports present: grep "PsychologyIcon\|WaterDropIcon\|HistoryIcon\|InsightsIcon" Sidebar.tsx returns 8 lines (import + navItems, 2 per icon)
- Visual verification via Playwright MCP: six distinct icon silhouettes visible in TopNav
</verification>

<success_criteria>
- All six nav icons have visually distinct silhouettes (no two from same visual family)
- Active/inactive color contract unchanged: inactive #4A4270 opacity 0.5, active #BF5AF2 opacity 1.0 with drop-shadow
- TypeScript build is clean
- User approves visual result via Playwright screenshot
</success_criteria>

<output>
After completion, create `.planning/phases/86-frontend-sidebar-icon-differentiation-make-nav-icons-visually-distinct-per-section/86-01-SUMMARY.md`
</output>
