---
phase: 67-apache-flink-real-time-stream-processing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - stock-prediction-platform/k8s/namespaces.yaml
  - stock-prediction-platform/k8s/flink/rbac.yaml
  - stock-prediction-platform/k8s/kafka/kafka-topic-processed-features.yaml
  - stock-prediction-platform/k8s/processing/configmap.yaml
  - stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/Dockerfile
  - stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/requirements.txt
  - stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/ohlcv_normalizer.py
  - stock-prediction-platform/tests/flink/__init__.py
  - stock-prediction-platform/tests/flink/test_ohlcv_normalizer.py
autonomous: true
requirements:
  - FLINK-01
  - FLINK-02
  - FLINK-05

must_haves:
  truths:
    - "Flink Kubernetes Operator Helm install command produces a running operator pod in the flink namespace"
    - "FlinkDeployment CRD is present in the cluster after operator install"
    - "processed-features KafkaTopic CR exists in storage namespace with 3 partitions and 24h retention"
    - "kafka-consumer ConfigMap no longer lists intraday-data as INTRADAY_TOPIC (Flink owns that path now)"
    - "ohlcv_normalizer.py filters fetch_mode=intraday, validates close > 0 and ticker IS NOT NULL, and writes to ohlcv_intraday via JDBC upsert sink with PRIMARY KEY (ticker, timestamp) NOT ENFORCED"
    - "OHLCV Normalizer Docker image builds cleanly inside minikube docker-env"
    - "Unit tests cover normalization filter logic and pass"
  artifacts:
    - path: "stock-prediction-platform/k8s/flink/rbac.yaml"
      provides: "ServiceAccount flink + ClusterRoleBinding for operator pod management"
    - path: "stock-prediction-platform/k8s/kafka/kafka-topic-processed-features.yaml"
      provides: "Strimzi KafkaTopic CR processed-features"
    - path: "stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/ohlcv_normalizer.py"
      provides: "PyFlink Table API job: Kafka source -> filter/validate -> JDBC upsert sink"
    - path: "stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/Dockerfile"
      provides: "Flink 1.19 image with PyFlink + JDBC + Kafka connector JARs"
    - path: "stock-prediction-platform/tests/flink/test_ohlcv_normalizer.py"
      provides: "Unit tests for normalization logic (no Flink runtime dependency)"
  key_links:
    - from: "ohlcv_normalizer.py"
      to: "ohlcv_intraday TimescaleDB hypertable"
      via: "JDBC connector with PRIMARY KEY NOT ENFORCED triggers INSERT ON CONFLICT DO UPDATE"
      pattern: "PRIMARY KEY.*NOT ENFORCED"
    - from: "ohlcv_normalizer.py"
      to: "kafka-kafka-bootstrap.storage.svc.cluster.local:9092"
      via: "KAFKA_BOOTSTRAP_SERVERS env var from flink-config ConfigMap"
      pattern: "KAFKA_BOOTSTRAP_SERVERS"
---

<objective>
Install the Flink Kubernetes Operator infrastructure and implement Job 1 — OHLCV Normalizer.

Purpose: Establish the flink namespace, operator, RBAC, and new processed-features Kafka topic. Implement the first PyFlink job that reads intraday-data, validates/normalizes OHLCV records, and upserts to TimescaleDB — replacing the batch kafka-consumer for the intraday path.

Output: Flink operator Helm install command documented in deploy-all.sh, flink namespace + RBAC manifests, processed-features KafkaTopic CR, kafka-consumer ConfigMap updated to drop intraday-data subscription, ohlcv_normalizer Docker image + FlinkDeployment CR, unit tests.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-apache-flink-real-time-stream-processing/67-RESEARCH.md

<interfaces>
<!-- Key patterns the executor must follow. Extracted from codebase. -->

From stock-prediction-platform/k8s/kafka/kafka-topics.yaml:
```yaml
# Existing Strimzi KafkaTopic pattern — use same structure for processed-features
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: <topic-name>
  namespace: storage
  labels:
    strimzi.io/cluster: kafka
spec:
  partitions: 3
  replicas: 1
  config:
    retention.ms: <ms>
    segment.bytes: 1073741824
```

From stock-prediction-platform/k8s/processing/configmap.yaml:
```yaml
# Current kafka-consumer ConfigMap — update INTRADAY_TOPIC to empty string
data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka-kafka-bootstrap.storage.svc.cluster.local:9092"
  KAFKA_GROUP_ID: "stock-consumer-group"
  INTRADAY_TOPIC: "intraday-data"   # <-- set to "" to remove intraday from batch consumer
  HISTORICAL_TOPIC: "historical-data"
```

From stock-prediction-platform/k8s/monitoring/grafana-deployment.yaml (RBAC + namespace pattern):
```yaml
# deploy-all.sh applies kubectl apply -f k8s/namespaces.yaml first
# Flink namespace must be added to k8s/namespaces.yaml following the existing pattern
apiVersion: v1
kind: Namespace
metadata:
  name: flink
  labels:
    app.kubernetes.io/part-of: stock-prediction-platform
```

From 67-RESEARCH.md — JDBC upsert sink (PRIMARY KEY triggers upsert mode):
```python
t_env.execute_sql("""
    CREATE TABLE ohlcv_intraday_sink (
        ticker STRING,
        `timestamp` TIMESTAMP(3),
        `open` DECIMAL(12, 4),
        ...
        PRIMARY KEY (ticker, `timestamp`) NOT ENFORCED
    ) WITH (
        'connector' = 'jdbc',
        'url' = '{PG_URL}',
        'table-name' = 'ohlcv_intraday',
        ...
    )
""")
```

From 67-RESEARCH.md — Kafka cross-namespace DNS:
- Kafka in storage namespace: kafka-kafka-bootstrap.storage.svc.cluster.local:9092
- PostgreSQL in storage namespace: postgresql.storage.svc.cluster.local:5432
- NOT "kafka:9092" (docker-compose hostname, wrong in K8s)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace, RBAC, Kafka topic, and kafka-consumer ConfigMap update</name>
  <files>
    stock-prediction-platform/k8s/namespaces.yaml,
    stock-prediction-platform/k8s/flink/rbac.yaml,
    stock-prediction-platform/k8s/kafka/kafka-topic-processed-features.yaml,
    stock-prediction-platform/k8s/processing/configmap.yaml
  </files>
  <action>
    1. Append flink namespace to k8s/namespaces.yaml following the existing namespace block pattern (apiVersion: v1, kind: Namespace, metadata.name: flink, labels: app.kubernetes.io/part-of: stock-prediction-platform). Use --- separator.

    2. Create k8s/flink/rbac.yaml with two documents separated by ---:
       - ServiceAccount named "flink" in namespace "flink"
       - ClusterRoleBinding named "flink-role-binding" binding ClusterRole "edit" to the flink ServiceAccount in namespace flink. Use apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: edit.

    3. Create k8s/kafka/kafka-topic-processed-features.yaml as a standalone KafkaTopic CR (do NOT add to kafka-topics.yaml — keep it separate for Phase 67 traceability):
       - name: processed-features
       - namespace: storage
       - label strimzi.io/cluster: kafka
       - partitions: 3, replicas: 1
       - config: retention.ms: 86400000 (24h), segment.bytes: 1073741824

    4. In k8s/processing/configmap.yaml, set INTRADAY_TOPIC to empty string "" (Flink Job 1 now owns the intraday-data topic; the batch kafka-consumer should only consume historical-data going forward). Leave all other fields unchanged.
  </action>
  <verify>
    kubectl apply --dry-run=client -f stock-prediction-platform/k8s/namespaces.yaml | grep -q "flink"
    kubectl apply --dry-run=client -f stock-prediction-platform/k8s/flink/rbac.yaml | grep -q "flink"
    kubectl apply --dry-run=client -f stock-prediction-platform/k8s/kafka/kafka-topic-processed-features.yaml | grep -q "processed-features"
    grep 'INTRADAY_TOPIC: ""' stock-prediction-platform/k8s/processing/configmap.yaml
  </verify>
  <done>flink namespace in namespaces.yaml; rbac.yaml creates ServiceAccount + ClusterRoleBinding; processed-features KafkaTopic CR valid; INTRADAY_TOPIC is empty string in processing ConfigMap</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OHLCV Normalizer — PyFlink job, Dockerfile, and unit tests</name>
  <files>
    stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/ohlcv_normalizer.py,
    stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/Dockerfile,
    stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/requirements.txt,
    stock-prediction-platform/tests/flink/__init__.py,
    stock-prediction-platform/tests/flink/test_ohlcv_normalizer.py
  </files>
  <behavior>
    Unit tests must NOT import pyflink (no Flink runtime in test environment). Extract pure Python normalization logic into a helper function and test that:
    - Test 1: record with fetch_mode="intraday", valid ticker, close > 0 passes filter → returns record unchanged
    - Test 2: record with fetch_mode="historical" → filtered out (returns None or excluded from result)
    - Test 3: record with close = 0 → filtered out
    - Test 4: record with ticker = None → filtered out
    - Test 5: record with close = None → filtered out
    - Test 6: vwap defaults to close value when vwap is None (normalization fallback)
    - Test 7: decimal precision — close = 1234.56789 rounds to 4dp → 1234.5679
  </behavior>
  <action>
    Create services/flink-jobs/ohlcv_normalizer/ directory structure.

    Write ohlcv_normalizer.py using PyFlink Table API (SQL DDL pattern from RESEARCH.md):
    - Import EnvironmentSettings, TableEnvironment from pyflink.table
    - Read KAFKA_BOOTSTRAP_SERVERS, POSTGRES_HOST, POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD from os.environ
    - Define a module-level pure Python function `should_include_record(ticker, close, fetch_mode)` that returns bool — this is what unit tests exercise
    - Create intraday_source Kafka table DDL:
      - columns: ticker STRING, `timestamp` TIMESTAMP_LTZ(3), `open` DECIMAL(12,4), high DECIMAL(12,4), low DECIMAL(12,4), close DECIMAL(12,4), adj_close DECIMAL(12,4), volume BIGINT, vwap DECIMAL(12,4), fetch_mode STRING
      - WATERMARK FOR `timestamp` AS `timestamp` - INTERVAL '10' SECONDS
      - connector=kafka, topic=intraday-data, properties.bootstrap.servers from env, properties.group.id=flink-ohlcv-normalizer, scan.startup.mode=group-offsets, format=json, json.ignore-parse-errors=true
    - Create ohlcv_intraday_sink JDBC table DDL:
      - same columns as above MINUS fetch_mode column
      - PRIMARY KEY (ticker, `timestamp`) NOT ENFORCED — this triggers upsert mode
      - connector=jdbc, url=jdbc:postgresql://{POSTGRES_HOST}:5432/{POSTGRES_DB}, table-name=ohlcv_intraday, username/password from env, sink.buffer-flush.max-rows=100, sink.buffer-flush.interval=2s
    - INSERT INTO ohlcv_intraday_sink SELECT ... FROM intraday_source WHERE fetch_mode = 'intraday' AND ticker IS NOT NULL AND close IS NOT NULL AND close > 0
    - End with t_env.execute("ohlcv-normalizer-job") — this blocks until job terminates

    Write Dockerfile:
    FROM flink:1.19
    - apt-get install -y python3 python3-pip python3-dev
    - pip3 install apache-flink==1.19.* (no feast here — Job 1 does not use it)
    - wget to /opt/flink/lib/ (NOT /opt/flink/usrlib/):
      * flink-sql-connector-kafka-3.2.0-1.19.jar from Maven Central
      * flink-connector-jdbc-3.2.0-1.19.jar from Maven Central
      * postgresql-42.7.3.jar from Maven Central
    - COPY ohlcv_normalizer.py /opt/flink/usrlib/ohlcv_normalizer.py
    - USER flink

    Write requirements.txt: apache-flink==1.19.*

    Create tests/flink/__init__.py (empty).

    Write tests/flink/test_ohlcv_normalizer.py:
    - Import sys, add services/flink-jobs/ohlcv_normalizer to sys.path (or use importlib) to import should_include_record without triggering pyflink imports
    - Use unittest.mock to patch any module-level imports that would cause pyflink to be imported
    - Alternatively: structure ohlcv_normalizer.py so normalization helpers live in a separate pure-Python module (e.g., normalizer_logic.py) that tests import directly — whichever approach avoids pyflink at test time
    - Implement all 7 test cases described in behavior block
  </action>
  <verify>
    pytest stock-prediction-platform/tests/flink/test_ohlcv_normalizer.py -v
  </verify>
  <done>7 unit tests pass; ohlcv_normalizer.py uses SQL DDL pattern with JDBC PRIMARY KEY upsert; Dockerfile present with JARs in /opt/flink/lib/</done>
</task>

<task type="auto">
  <name>Task 3: FlinkDeployment CR for OHLCV Normalizer + flink-config ConfigMap</name>
  <files>
    stock-prediction-platform/k8s/flink/flink-config-configmap.yaml,
    stock-prediction-platform/k8s/flink/flinkdeployment-ohlcv-normalizer.yaml
  </files>
  <action>
    Create k8s/flink/flink-config-configmap.yaml in namespace flink:
    data:
      KAFKA_BOOTSTRAP_SERVERS: "kafka-kafka-bootstrap.storage.svc.cluster.local:9092"
      POSTGRES_HOST: "postgresql.storage.svc.cluster.local"
      POSTGRES_DB: "stockdb"
      POSTGRES_USER: "stockuser"
      FEAST_REPO_PATH: "/opt/feast"

    Create k8s/flink/flinkdeployment-ohlcv-normalizer.yaml:
    - apiVersion: flink.apache.org/v1beta1, kind: FlinkDeployment
    - metadata.name: ohlcv-normalizer, namespace: flink
    - spec.image: stock-flink-ohlcv-normalizer:latest
    - spec.imagePullPolicy: Never (minikube local image)
    - spec.flinkVersion: v1_19
    - spec.serviceAccount: flink
    - spec.flinkConfiguration:
        taskmanager.numberOfTaskSlots: "2"
        metrics.reporter.prom.factory.class: org.apache.flink.metrics.prometheus.PrometheusReporterFactory
        metrics.reporter.prom.port: "9249"
        state.backend: rocksdb
        state.backend.incremental: "true"
        state.checkpoints.dir: s3://model-artifacts/flink-checkpoints/ohlcv-normalizer
        s3.endpoint: http://minio.storage.svc.cluster.local:9000
        s3.path.style.access: "true"
        execution.checkpointing.interval: "30s"
        execution.checkpointing.mode: EXACTLY_ONCE
        restart-strategy.type: exponential-delay
        restart-strategy.exponential-delay.initial-backoff: 1s
        restart-strategy.exponential-delay.max-backoff: 60s
    - spec.jobManager.resource: memory 1024m, cpu 0.5
    - spec.taskManager.resource: memory 1024m, cpu 0.5
    - spec.podTemplate.metadata.annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9249"
        prometheus.io/path: "/"
    - spec.podTemplate.spec.containers[name=flink-main-container]:
        env: [{name: ENABLE_BUILT_IN_PLUGINS, value: "flink-s3-fs-presto-1.19.0.jar"}]
        envFrom: [{configMapRef: {name: flink-config}}, {secretRef: {name: stock-platform-secrets}}]
        (stock-platform-secrets carries POSTGRES_PASSWORD — same secret already copied to storage/ingestion/processing/ml namespaces; it must also be copied to flink namespace in deploy-all.sh — document this requirement in a comment in the YAML)
    - spec.job:
        entryClass: "org.apache.flink.client.python.PythonDriver"
        args: ["-pyclientexec", "/usr/local/bin/python3", "-py", "/opt/flink/usrlib/ohlcv_normalizer.py"]
        parallelism: 1
        upgradeMode: stateful
        state: running

    Note on s3.access.key / s3.secret.key: These must come from the minio-secrets Secret (MINIO_ROOT_USER / MINIO_ROOT_PASSWORD). Add envFrom secretRef for minio-secrets as well, and use Flink config key references. Since flinkConfiguration values are strings (not env vars), add s3.access.key and s3.secret.key as literal placeholders with a comment noting they will be overridden by minio-secrets env injection — or simply omit them from flinkConfiguration and rely on the AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY env vars being set from minio-secrets (the flink-s3-fs-presto plugin respects AWS env vars).
  </action>
  <verify>
    kubectl apply --dry-run=client -f stock-prediction-platform/k8s/flink/flinkdeployment-ohlcv-normalizer.yaml 2>&1 | grep -E "error|Error" || echo "dry-run OK (CRD may not be installed yet)"
    grep -q "state.backend: rocksdb" stock-prediction-platform/k8s/flink/flinkdeployment-ohlcv-normalizer.yaml
    grep -q "PRIMARY KEY" stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/ohlcv_normalizer.py
  </verify>
  <done>FlinkDeployment CR written with RocksDB checkpoint config, Prometheus annotations, minikube imagePullPolicy Never; flink-config ConfigMap created with cross-namespace DNS addresses</done>
</task>

</tasks>

<verification>
pytest stock-prediction-platform/tests/flink/test_ohlcv_normalizer.py -v
kubectl apply --dry-run=client -f stock-prediction-platform/k8s/flink/rbac.yaml
kubectl apply --dry-run=client -f stock-prediction-platform/k8s/kafka/kafka-topic-processed-features.yaml
grep 'INTRADAY_TOPIC: ""' stock-prediction-platform/k8s/processing/configmap.yaml
grep -q "flink" stock-prediction-platform/k8s/namespaces.yaml
grep -q "PRIMARY KEY" stock-prediction-platform/services/flink-jobs/ohlcv_normalizer/ohlcv_normalizer.py
grep -q "rocksdb" stock-prediction-platform/k8s/flink/flinkdeployment-ohlcv-normalizer.yaml
</verification>

<success_criteria>
- flink namespace entry in namespaces.yaml
- k8s/flink/rbac.yaml: ServiceAccount flink + ClusterRoleBinding to ClusterRole edit
- k8s/kafka/kafka-topic-processed-features.yaml: 3 partitions, 24h retention
- k8s/processing/configmap.yaml: INTRADAY_TOPIC is ""
- ohlcv_normalizer.py: Kafka DDL source + JDBC upsert sink with PRIMARY KEY NOT ENFORCED, filter on fetch_mode=intraday, close > 0, ticker NOT NULL
- Dockerfile: FROM flink:1.19, JARs in /opt/flink/lib/, PyFlink installed
- FlinkDeployment CR: RocksDB + MinIO checkpoint, Prometheus annotations, stateful upgrade mode
- Unit tests: 7 tests pass without pyflink runtime installed
</success_criteria>

<output>
After completion, create `.planning/phases/67-apache-flink-real-time-stream-processing/67-01-SUMMARY.md`
</output>
