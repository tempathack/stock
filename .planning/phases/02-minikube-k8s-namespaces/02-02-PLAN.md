---
phase: 02-minikube-k8s-namespaces
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Minikube cluster is Running with docker driver"
    - "All 5 namespaces (ingestion, processing, storage, ml, frontend) are Active in the cluster"
    - "Addons ingress, metrics-server, and dashboard are enabled"
    - "setup-minikube.sh runs end-to-end with exit code 0"
    - "deploy-all.sh runs end-to-end with exit code 0"
    - "setup-minikube.sh is idempotent — second run skips start and still exits 0"
  artifacts: []
  key_links:
    - from: "stock-prediction-platform/scripts/setup-minikube.sh"
      to: "minikube cluster"
      via: "minikube start and kubectl apply"
      pattern: "Cluster ready"
    - from: "stock-prediction-platform/scripts/deploy-all.sh"
      to: "minikube cluster"
      via: "kubectl apply"
      pattern: "Deployment complete"
---

<objective>
Execute both infrastructure scripts against the live minikube cluster and verify all phase requirements are met.

Purpose: Scripts written in Plan 01 must be proven to work against the real cluster. This plan runs them and verifies the observable outcomes match all INFRA requirements.

Output: Live cluster with 5 namespaces, verified by human.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-minikube-k8s-namespaces/02-CONTEXT.md
@.planning/phases/02-minikube-k8s-namespaces/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute setup-minikube.sh and deploy-all.sh</name>
  <files></files>
  <read_first>
    - stock-prediction-platform/scripts/setup-minikube.sh (the script to execute)
    - stock-prediction-platform/scripts/deploy-all.sh (the script to execute)
    - .planning/phases/02-minikube-k8s-namespaces/02-01-SUMMARY.md (what was implemented)
  </read_first>
  <action>
Run the infrastructure scripts in order and capture all output:

1. **Run setup-minikube.sh:**
   ```
   cd /home/tempa/Desktop/priv-project/stock-prediction-platform
   bash scripts/setup-minikube.sh
   ```
   This should: check prereqs, start minikube (or skip if running), wait for node readiness, enable addons, apply namespaces, and print "Cluster ready".

   If it fails, read the error output carefully and fix the script in-place. Common issues:
   - minikube status format string may need adjustment
   - Addon enable may print warnings (non-fatal, should not fail with pipefail)
   - Namespace apply should be idempotent (already exists = OK)

2. **Run deploy-all.sh:**
   ```
   bash scripts/deploy-all.sh
   ```
   This should: check kubectl and minikube prereqs, apply namespaces, and print "Deployment complete".

3. **Run setup-minikube.sh a SECOND time** to verify idempotency:
   ```
   bash scripts/setup-minikube.sh
   ```
   Second run should skip the `minikube start` step and print "already running" message.

4. **Collect verification data:**
   ```
   minikube status
   kubectl get namespaces
   kubectl get nodes
   minikube addons list | grep -E "ingress|metrics-server|dashboard"
   ```
  </action>
  <verify>
    <automated>minikube status | grep -q "Running" && kubectl get ns ingestion processing storage ml frontend -o name | wc -l | grep -q "5" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `minikube status` output contains "host: Running" and "kubelet: Running"
    - `kubectl get ns ingestion -o jsonpath='{.status.phase}'` outputs "Active"
    - `kubectl get ns processing -o jsonpath='{.status.phase}'` outputs "Active"
    - `kubectl get ns storage -o jsonpath='{.status.phase}'` outputs "Active"
    - `kubectl get ns ml -o jsonpath='{.status.phase}'` outputs "Active"
    - `kubectl get ns frontend -o jsonpath='{.status.phase}'` outputs "Active"
    - `minikube addons list` shows ingress, metrics-server, and dashboard as enabled
    - setup-minikube.sh second run exits 0 and output contains "already running" (idempotency)
    - deploy-all.sh exits 0 and output contains "Deployment complete"
  </acceptance_criteria>
  <done>Both scripts execute successfully. Minikube cluster is Running. All 5 namespaces are Active. All 3 addons are enabled. setup-minikube.sh is idempotent on second run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cluster state</name>
  <what-built>
Local Minikube Kubernetes cluster with 5 namespaces (ingestion, processing, storage, ml, frontend) bootstrapped via setup-minikube.sh and deploy-all.sh scripts.
  </what-built>
  <how-to-verify>
1. Run `minikube status` — expect host: Running, kubelet: Running, apiserver: Running
2. Run `kubectl get namespaces` — expect ingestion, processing, storage, ml, frontend all with Active status
3. Run `minikube addons list | grep -E "ingress|metrics-server|dashboard"` — all three should show "enabled"
4. Run `bash stock-prediction-platform/scripts/setup-minikube.sh` — should skip start (prints "already running") and exit 0
5. Run `bash stock-prediction-platform/scripts/deploy-all.sh` — should apply namespaces and print "Deployment complete"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `minikube status` shows Running
2. `kubectl get ns ingestion processing storage ml frontend` — all 5 exist and are Active
3. `minikube addons list` shows ingress, metrics-server, dashboard enabled
4. Both scripts exit 0 on execution
5. setup-minikube.sh is idempotent (second run skips start)
</verification>

<success_criteria>
Live Minikube cluster is Running with all 5 namespaces Active, all 3 addons enabled, and both scripts proven to execute successfully. Human has verified cluster state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-minikube-k8s-namespaces/02-02-SUMMARY.md`
</output>
