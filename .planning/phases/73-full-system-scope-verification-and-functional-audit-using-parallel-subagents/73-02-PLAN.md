---
phase: 73-full-system-scope-verification-and-functional-audit-using-parallel-subagents
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - .planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "Domain 1 section of 73-AUDIT.md is populated with findings — not [PENDING]"
    - "Each API router file is confirmed present or flagged as missing"
    - "Rate limiting (slowapi), deep health checks, WebSocket endpoints, and A/B results endpoint are each confirmed or flagged as gap"
    - "Test coverage for API domain is assessed (count of test files and basic coverage assessment)"
    - "Phase 70 streaming-features endpoint existence is confirmed by file inspection"
    - "Phase 71 /ws/sentiment/{ticker} WebSocket existence is confirmed by file inspection"
  artifacts:
    - path: ".planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md"
      provides: "Domain 1 FastAPI API section populated with gap findings"
  key_links:
    - from: "services/api/app/routers/"
      to: "73-AUDIT.md Domain 1 section"
      via: "file inspection of each router for required endpoints and patterns"
    - from: "services/api/app/metrics.py"
      to: "73-AUDIT.md Domain 1 section"
      via: "grep for custom Prometheus metric names"
---

<objective>
Inspect all FastAPI router, service, and test files and write findings into the "Domain 1: FastAPI API Layer" section of 73-AUDIT.md. This is a read-only audit — no code changes.

Purpose: The API layer is the integration point for all data paths (ingest, predict, WebSocket, streaming features, sentiment). Confirming all required endpoints exist and are wired to live data (not stubs) validates that the platform's external interface is complete.

Output: Domain 1 section of 73-AUDIT.md populated with: files inspected, requirements satisfied, gaps, stubs detected, missing tests, wiring issues.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect FastAPI routers, services, and tests — write Domain 1 findings to AUDIT.md</name>
  <files>.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md</files>

  <read_first>
    - .planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md — read to understand the Domain 1 section structure before writing
    - Run: ls /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/app/routers/ 2>/dev/null
    - Run: ls /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/app/services/ 2>/dev/null
    - Run: ls /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/tests/ 2>/dev/null
    - Read each router file found (health.py, ingest.py, predict.py, models.py, market.py, analytics.py, backtest.py, ws.py or equivalent)
    - Read services/api/app/main.py — check router registrations and middleware
    - Read services/api/app/metrics.py — check Prometheus metric definitions
    - Run: grep -r "raise NotImplementedError\|pass$\|return None\|TODO\|FIXME\|PLACEHOLDER" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/app/ --include="*.py" -l 2>/dev/null
  </read_first>

  <action>
THIS IS AN AUDIT TASK. Do NOT modify any file in `stock-prediction-platform/`. Only read files and write findings to 73-AUDIT.md.

Inspect the following specific requirements for Domain 1 (FastAPI API):

**Endpoints to verify (check each router file):**

1. `GET /health` — API-01: basic health response. Look in `routers/health.py`. Confirm: endpoint exists, returns JSON with status field.
2. `POST /ingest/intraday` and `POST /ingest/historical` — API-05, API-06: ingestion endpoints. Look in `routers/ingest.py`. Confirm: exist, call yahoo_finance service and kafka_producer.
3. `POST /predict/{ticker}` with `?horizon=1|7|30` — API-07, ADVML-05: multi-horizon prediction. Look in `routers/predict.py`. Confirm: horizon query param exists, calls live inference (not a stub returning hardcoded values).
4. `GET /predict/bulk` — API-08: bulk prediction. Look in `routers/predict.py`. Confirm: endpoint exists.
5. `GET /models/*` endpoints — API-09, API-10: model listing and detail. Look in `routers/models.py`. Confirm: at minimum `/models/` and `/models/{name}` exist.
6. `GET /models/ab-results` — PROD-08: A/B test results. Look in `routers/models.py` or `routers/predict.py`. Confirm: endpoint exists and returns ab test metrics.
7. `GET /market/overview` and `GET /market/indicators/{ticker}` — API-11, API-12. Look in `routers/market.py`. Confirm: both exist.
8. `GET /market/streaming-features/{ticker}` — Phase 70 deliverable. Look in `routers/market.py`. Confirm: endpoint exists, calls Feast online store via `get_streaming_features()`, returns JSON with fields including `ema_20`, `rsi_14`, `macd_signal`, `available` flag.
9. `GET /backtest/{ticker}` — FENH-04. Look in `routers/backtest.py` or `routers/market.py`. Confirm: endpoint exists.
10. `WebSocket /ws/prices` — FENH-01. Look in `routers/ws.py` or `main.py`. Confirm: WebSocket endpoint exists.
11. `WebSocket /ws/sentiment/{ticker}` — Phase 71 deliverable. Look in `routers/ws.py` or `routers/sentiment.py`. Confirm: WebSocket endpoint exists and sources from Feast Redis.
12. `GET /analytics/*` endpoints — Phase 69 deliverable. Look in `routers/analytics.py`. Confirm: at least one analytics endpoint exists.

**Middleware to verify:**
- `slowapi` rate limiting — PROD-04: Check `main.py` for `from slowapi import Limiter` or `SlowAPIMiddleware` import and registration.
- Deep health checks — PROD-05: Check `routers/health.py` for DB, Redis, Kafka connectivity checks (not just `{"status": "ok"}`).

**Metrics to verify:**
- `metrics.py` contains at least 3 custom Prometheus Counter or Histogram metrics — MON-02. Look for `Counter(`, `Histogram(`, `Gauge(` patterns.

**Stub detection:**
- Run the grep command from read_first and list any files with stubs found.

**Test coverage:**
- List test files found in `services/api/tests/`. Note: passing or failing is not assessable without running them. Just confirm file existence and rough count.

After inspecting all files, **replace the `[PENDING — Plan 73-02 populates this section]` marker** in the Domain 1 section of 73-AUDIT.md with a structured report using this format:

```
**Status:** [COMPLETE / PARTIAL / CRITICAL-GAPS]
**Files Inspected:** [count]
**Test Files Found:** [list]

#### Satisfied Requirements
| REQ-ID | Evidence | File |
|--------|----------|------|
| API-01 | GET /health endpoint present | routers/health.py |
| ... | ... | ... |

#### Gaps Found
| REQ-ID | Gap Class | Description | File Expected |
|--------|-----------|-------------|---------------|
| [ID] | [CRITICAL/MISSING-TEST/STUB/ORPHANED] | [what's missing or broken] | [expected path] |

#### Stubs Detected
[Files with pass/return None/TODO/FIXME patterns — list paths]

#### Wiring Issues
[Missing imports, disconnected handler chains, or endpoint that exists but calls a stub service]

#### Phase-Specific Checks
- Phase 70 /market/streaming-features/{ticker}: [CONFIRMED / NOT FOUND / STUB]
- Phase 71 /ws/sentiment/{ticker}: [CONFIRMED / NOT FOUND / STUB]
- Phase 69 /analytics/* endpoints: [CONFIRMED / NOT FOUND]
- PROD-04 slowapi rate limiting: [CONFIRMED / NOT FOUND]
- PROD-05 deep health checks: [CONFIRMED / SHALLOW — only status:ok / NOT FOUND]
- MON-02 custom Prometheus metrics (>=3): [CONFIRMED — N metrics / NOT FOUND]
```

Do not remove any other content from 73-AUDIT.md. Only replace the [PENDING] marker in the Domain 1 section.
  </action>

  <verify>
    <automated>grep -v "PENDING" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md | grep "Domain 1" | head -3</automated>
    <automated>grep "Phase 70.*streaming-features\|streaming-features.*Phase 70\|CONFIRMED\|NOT FOUND" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md | head -5</automated>
    <automated>grep "Files Inspected:" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - 73-AUDIT.md Domain 1 section no longer contains `[PENDING — Plan 73-02 populates this section]`
    - 73-AUDIT.md contains `Files Inspected:` in Domain 1 section
    - 73-AUDIT.md contains `Phase 70.*streaming-features` assessment line (either CONFIRMED, NOT FOUND, or STUB)
    - 73-AUDIT.md contains `Phase 71.*sentiment` assessment line
    - 73-AUDIT.md contains `PROD-04` or `slowapi` assessment line in Domain 1 section
    - 73-AUDIT.md still contains the 5 remaining `[PENDING` markers for domains 2–6 (grep -c "PENDING" returns 5)
  </acceptance_criteria>

  <done>Domain 1 (FastAPI API) section of 73-AUDIT.md is populated with findings. All required API endpoints assessed. Phase 70 and Phase 71 deliverables confirmed or flagged as gaps. Rate limiting, deep health checks, and Prometheus metrics assessed.</done>
</task>

</tasks>

<verification>
grep -c "PENDING" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
# must return 5 (domains 2-6 still pending)

grep "Domain 1" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
# must show populated section header
</verification>

<success_criteria>
Domain 1 section populated in 73-AUDIT.md with:
1. All 12 required endpoints assessed (present/absent/stub)
2. Phase 70 and Phase 71 endpoint status confirmed by code inspection
3. Slowapi rate limiting and deep health check confirmed or flagged
4. Prometheus metrics count assessed
5. Stub detection results recorded
6. Test file count recorded
</success_criteria>

<output>
After completion, create `.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-02-SUMMARY.md`
</output>
