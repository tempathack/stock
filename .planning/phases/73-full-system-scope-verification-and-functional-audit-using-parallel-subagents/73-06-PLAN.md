---
phase: 73-full-system-scope-verification-and-functional-audit-using-parallel-subagents
plan: 06
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - .planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "Domain 5 section of 73-AUDIT.md is populated with findings — not [PENDING]"
    - "prometheus-configmap.yaml flink-jobs scrape job confirmed present or flagged (Phase 72-01)"
    - "Grafana datasource UID lowercase 'prometheus' confirmed or flagged (Phase 72)"
    - "Flink dashboard panel count (expected 10) confirmed or flagged (Phase 72-02)"
    - "All 3+ custom Prometheus metrics in metrics.py confirmed or flagged (MON-02)"
    - "Loki datasource in Grafana confirmed or flagged (MON-10)"
    - "Alert rules for drift/API/Kafka confirmed or flagged (MON-08)"
  artifacts:
    - path: ".planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md"
      provides: "Domain 5 Observability section populated with gap findings"
  key_links:
    - from: "k8s/monitoring/prometheus-configmap.yaml"
      to: "73-AUDIT.md Domain 5 section"
      via: "grep for flink-jobs scrape job and job name pattern"
    - from: "k8s/monitoring/grafana-dashboard-*.yaml ConfigMaps"
      to: "73-AUDIT.md Domain 5 section"
      via: "count panels in Flink dashboard ConfigMap and verify datasource UID"
---

<objective>
Inspect all observability configuration files (Prometheus ConfigMaps, Grafana dashboard ConfigMaps, Loki/Promtail configs, Alertmanager config, API metrics.py) and write findings into the "Domain 5: Observability" section of 73-AUDIT.md. This is a read-only audit — no code changes.

Purpose: Phase 72 was the most recent phase and specifically addressed Grafana/Prometheus issues. Confirming the flink-jobs scrape job, datasource UID pin, and 10-panel Flink dashboard validates that Phase 72 work landed correctly. The broader observability check covers MON-01–10 requirements.

Output: Domain 5 section of 73-AUDIT.md populated with all findings.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect observability configs and API metrics — write Domain 5 findings to AUDIT.md</name>
  <files>.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md</files>

  <read_first>
    - .planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md — read Domain 5 section structure
    - Run: ls /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/ 2>/dev/null
    - Read k8s/monitoring/prometheus-configmap.yaml — full file
    - Read k8s/monitoring/grafana-datasource-configmap.yaml — full file (check datasource UID)
    - Run: ls /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/ | grep grafana-dashboard 2>/dev/null
    - Read each grafana-dashboard-*.yaml ConfigMap — count panels (look for "title" keys in JSON), confirm datasource references
    - Read k8s/monitoring/loki-configmap.yaml — confirm Loki config exists
    - Read k8s/monitoring/promtail-configmap.yaml — confirm Promtail DaemonSet config
    - Read k8s/monitoring/alertmanager-configmap.yaml — check alert rules
    - Run: grep -r "scrape_configs\|job_name\|flink" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/prometheus-configmap.yaml 2>/dev/null
    - Read services/api/app/metrics.py — check Counter/Histogram/Gauge definitions
    - Run: grep -c "\"title\"" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/grafana-dashboard-flink.yaml 2>/dev/null || grep -c "\"title\"" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/grafana-dashboard-flink-*.yaml 2>/dev/null
    - Run: grep "uid\|datasource" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/monitoring/grafana-datasource-configmap.yaml 2>/dev/null
  </read_first>

  <action>
THIS IS AN AUDIT TASK. Do NOT modify any file in `stock-prediction-platform/`. Only read files and write findings to 73-AUDIT.md.

Inspect the following specific requirements for Domain 5 (Observability):

**MON-01: Prometheus deployed (k8s/monitoring/)**

- Look for `prometheus-deployment.yaml` or equivalent in k8s/monitoring/
- Confirm: Prometheus K8s deployment manifest exists

**MON-02: Custom metrics in API (services/api/app/metrics.py)**

- Read metrics.py and count `Counter(`, `Histogram(`, `Gauge(` definitions
- Minimum 3 custom metrics required (e.g., prediction_requests_total, model_inference_latency_seconds, drift_detection_alerts_total)
- Record the exact metric names found

**MON-03–04: Prometheus scrape configuration**

- Read prometheus-configmap.yaml `scrape_configs` section
- Confirm `job_name: fastapi` or equivalent API scrape job exists
- **Phase 72-01 critical check:** Confirm `job_name: flink-jobs` or `flink` scrape job exists in prometheus-configmap.yaml. This was the Phase 72-01 deliverable. Record the exact job_name if found.

**MON-05–07: Grafana dashboards (k8s/monitoring/grafana-dashboard-*.yaml)**

- List all Grafana dashboard ConfigMap files found
- For each dashboard: note name and approximate panel count (grep -c "\"title\"" gives panel count)
- Expected dashboards (from Phase 38): api-health, ml-perf, kafka, drift
- **Phase 72-02 critical check:** Flink dashboard ConfigMap — count panels. Expected: 10. Record actual count.
- Verify datasource references in dashboard JSON use `${DS_PROMETHEUS}` or the pinned UID `prometheus` (lowercase)

**MON-08: Alert rules (k8s/monitoring/alertmanager-configmap.yaml or prometheus alert rules)**

- Read alertmanager-configmap.yaml — confirm alert rules are defined, not just webhook config
- Look for: drift alert, API error rate alert, Kafka lag alert
- Note: alert rules may be in a separate `prometheus-rules.yaml` — check ls output

**MON-09: Structured logging (structlog)**

Per Phase 39 decisions: structlog JSON format, Loki+Promtail DaemonSet
- Confirm loki-configmap.yaml exists and has Loki configuration (storage backend, retention)
- Confirm promtail-configmap.yaml exists and has `pipeline_stages` for JSON log parsing

**MON-10: Loki datasource in Grafana**

- Read grafana-datasource-configmap.yaml — confirm it has TWO datasources: Prometheus and Loki
- Check for `type: loki` datasource entry
- **Grafana datasource UID pin (Phase 72):** Confirm the Prometheus datasource has `uid: prometheus` (lowercase, not `Prometheus` with capital P or a UUID). This was the Phase 72 fix.

After inspecting all files, **replace the `[PENDING — Plan 73-06 populates this section]` marker** in the Domain 5 section of 73-AUDIT.md with:

```
**Status:** [COMPLETE / PARTIAL / CRITICAL-GAPS]
**Files Inspected:** [count]
**Grafana Dashboard ConfigMaps Found:** [list with panel counts]

#### Satisfied Requirements
| REQ-ID | Evidence | File |
|--------|----------|------|
| MON-01 | Prometheus deployment manifest present | k8s/monitoring/[file] |
| MON-02 | N custom metrics: [names] | services/api/app/metrics.py |
| ... | ... | ... |

#### Gaps Found
| REQ-ID | Gap Class | Description | File Expected |
|--------|-----------|-------------|---------------|

#### Wiring Issues
[e.g., Grafana dashboard references datasource uid "Prometheus" (capital) instead of "prometheus" (lowercase)]

#### Phase 72 Audit
- Phase 72-01 flink-jobs scrape job in prometheus-configmap.yaml: [CONFIRMED — job_name: X / NOT FOUND]
- Phase 72 datasource UID pinned to lowercase "prometheus": [CONFIRMED / NOT CONFIRMED — uid is: X]
- Phase 72-02 Flink dashboard panel count: [N panels found — expected 10 / FILE NOT FOUND]

#### Phase-Specific Checks
- MON-02 custom metrics count: [N metrics in metrics.py — names: X, Y, Z]
- MON-03 API scrape job: [CONFIRMED — job_name: X / NOT FOUND]
- MON-04 flink scrape job (Phase 72-01): [CONFIRMED / NOT FOUND]
- MON-05–07 dashboard ConfigMaps: [api-health: N panels, ml-perf: N panels, kafka: N panels, drift: N panels, flink: N panels]
- MON-08 alert rules (drift/API/Kafka): [CONFIRMED — N rules found / NOT FOUND / PARTIAL]
- MON-09 Loki + Promtail configs: [CONFIRMED / NOT FOUND]
- MON-10 Loki datasource in Grafana: [CONFIRMED / NOT FOUND]
```

Do not remove any other content from 73-AUDIT.md. Only replace the [PENDING] marker in the Domain 5 section.
  </action>

  <verify>
    <automated>grep -v "PENDING" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md | grep "Domain 5" | head -3</automated>
    <automated>grep "Phase 72-01 flink-jobs\|flink.*scrape\|flink-jobs scrape" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md | head -3</automated>
    <automated>grep -c "PENDING" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md</automated>
  </verify>

  <acceptance_criteria>
    - 73-AUDIT.md Domain 5 section no longer contains `[PENDING — Plan 73-06 populates this section]`
    - 73-AUDIT.md contains `Phase 72-01` assessment line in Domain 5
    - 73-AUDIT.md contains `datasource UID` or `datasource.*pinned` assessment line in Domain 5
    - 73-AUDIT.md contains `Flink dashboard panel count` or `flink.*panels` assessment line in Domain 5
    - 73-AUDIT.md contains `MON-02` or `custom metrics count` assessment in Domain 5
    - grep -c "PENDING" returns 1 (domain 6 still pending)
  </acceptance_criteria>

  <done>Domain 5 (Observability) section of 73-AUDIT.md populated. Phase 72 deliverables (flink scrape job, datasource UID pin, 10-panel Flink dashboard) confirmed or flagged. All MON-01–10 requirements assessed. Loki datasource in Grafana confirmed or flagged.</done>
</task>

</tasks>

<verification>
grep -c "PENDING" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
# must return 1

grep "Phase 72-01" /home/tempa/Desktop/priv-project/.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-AUDIT.md
# must show assessment line
</verification>

<success_criteria>
Domain 5 section populated in 73-AUDIT.md with:
1. Prometheus custom metrics count and names from metrics.py
2. Prometheus scrape job count (including flink-jobs — Phase 72-01)
3. All Grafana dashboard ConfigMaps listed with panel counts
4. Flink dashboard panel count confirmed (expected 10 — Phase 72-02)
5. Grafana datasource UID confirmed as lowercase 'prometheus' (Phase 72 fix)
6. Loki datasource in Grafana confirmed or flagged (MON-10)
7. Alert rules assessed (MON-08)
</success_criteria>

<output>
After completion, create `.planning/phases/73-full-system-scope-verification-and-functional-audit-using-parallel-subagents/73-06-SUMMARY.md`
</output>
