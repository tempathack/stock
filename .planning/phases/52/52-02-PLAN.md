---
phase: 52-model-registry-s3-backend
plan: 02
type: execute
wave: 5
depends_on: [52-01]
files_modified:
  - ml/tests/test_registry.py
  - ml/tests/conftest.py
autonomous: true
scope_exclusions:
  - StorageBackend implementation (completed in plan 52-01)
  - ModelRegistry refactor (completed in plan 52-01)
  - requirements.txt changes (completed in plan 52-01)
  - K8s ConfigMap changes (completed in plan 52-01)
  - Integration tests against live MinIO (manual verification only)
  - Training/drift pipeline test updates (phase 53)
  - Performance or load testing
  - E2E tests in tests/integration/ (those test the API service, not ml/ internals)
must_haves:
  truths:
    # ━━━━━━━━━━ TEST DEPENDENCY: moto ━━━━━━━━━━

    # --- ml/requirements.txt (MODIFIED — or dev-requirements.txt if exists) ---
    - Add "moto[s3]>=5.0.0" under a new "# Testing" section at the bottom
    - moto provides in-process S3 mock — no external services needed for unit tests
    - Alternative: if the project uses a separate test requirements file, add there instead

    # ━━━━━━━━━━ SHARED FIXTURES ━━━━━━━━━━

    # --- ml/tests/conftest.py (MODIFIED or CREATED) ---
    - Add pytest fixtures shared across test modules

    # Fixture: mock_s3 (autouse=False):
    - Uses moto.mock_aws() context manager (moto v5 API)
    - Creates a real boto3 S3 client pointing to mocked endpoint
    - Creates bucket "model-artifacts" in the mock
    - Sets environment variables:
      - STORAGE_BACKEND=s3
      - MINIO_ENDPOINT=https://s3.amazonaws.com (moto default — not used but needed)
      - MINIO_ROOT_USER=testing
      - MINIO_ROOT_PASSWORD=testing
      - MINIO_BUCKET_MODELS=model-artifacts
      - AWS_ACCESS_KEY_ID=testing
      - AWS_SECRET_ACCESS_KEY=testing
      - AWS_DEFAULT_REGION=us-east-1
    - Yields the mock context
    - On teardown: restores original env vars

    # Fixture: s3_backend (depends on mock_s3):
    - Returns S3StorageBackend(bucket="model-artifacts", endpoint_url=None)
    - Since moto intercepts all boto3 calls, endpoint_url is not needed
    - Used directly in storage backend tests

    # Fixture: s3_registry (depends on mock_s3):
    - Returns ModelRegistry(backend=S3StorageBackend(bucket="model-artifacts"))
    - Uses dependency injection — ModelRegistry receives S3 backend
    - Used in registry tests that need S3 behavior

    # Existing fixtures (sample_pipeline, sample_result, sample_features) remain in test_registry.py unchanged

    # ━━━━━━━━━━ STORAGE BACKEND UNIT TESTS ━━━━━━━━━━

    # --- ml/tests/test_storage_backends.py (NEW) ---
    # Tests for LocalStorageBackend:

    # class TestLocalStorageBackend:
    - test_write_read_bytes: write binary data, read back, assert equal
    - test_write_read_json: write dict, read back, assert equal
    - test_read_nonexistent_raises: read missing key → FileNotFoundError
    - test_list_keys: write 3 files under prefix, list_keys returns all 3
    - test_list_keys_empty: list_keys on empty prefix → empty list
    - test_delete_file: write, delete, exists returns False
    - test_delete_nonexistent: delete missing key → returns False
    - test_exists_true: write, exists → True
    - test_exists_false: exists on missing key → False
    - test_nested_keys: write to "a/b/c/file.txt", read back succeeds
    - Uses tmp_path fixture for isolated filesystem

    # Tests for S3StorageBackend (using moto mock):

    # class TestS3StorageBackend:
    - test_write_read_bytes: write binary data via S3, read back, assert equal
    - test_write_read_json: write dict via S3, read back, assert equal
    - test_read_nonexistent_raises: read missing key → FileNotFoundError
    - test_list_keys: write 3 objects, list_keys returns all 3
    - test_list_keys_with_prefix: write to nested prefix, list only that prefix
    - test_list_keys_empty: list_keys on empty bucket → empty list
    - test_delete_object: write, delete, exists returns False
    - test_exists_true: write, exists → True
    - test_exists_false: exists on missing key → False
    - test_prefix_isolation: S3StorageBackend with prefix="models" writes to models/ namespace
    - Uses mock_s3 fixture from conftest.py

    # Tests for create_storage_backend factory:

    # class TestCreateStorageBackend:
    - test_default_is_local: with STORAGE_BACKEND unset, returns LocalStorageBackend
    - test_env_var_local: set STORAGE_BACKEND=local, returns LocalStorageBackend
    - test_env_var_s3: set STORAGE_BACKEND=s3 (with mock_s3 fixture), returns S3StorageBackend
    - test_explicit_override: create_storage_backend("local") ignores env var
    - test_unknown_raises: create_storage_backend("redis") → ValueError
    - Uses monkeypatch to set/unset env vars

    # ━━━━━━━━━━ REGISTRY TEST PARAMETRIZATION ━━━━━━━━━━

    # --- ml/tests/test_registry.py (MODIFIED) ---
    # Strategy: parametrize all existing test classes to run against BOTH backends

    # Approach — parametrized registry fixture:
    - Add a pytest.fixture "registry" that is parametrized over ["local", "s3"]
    - "local" variant: ModelRegistry(base_dir=str(tmp_path / "registry")) — same as current
    - "s3" variant: uses s3_registry fixture from conftest.py (ModelRegistry with S3StorageBackend)
    - This makes ALL existing tests automatically run against both backends
    - pytest.param ids: "local-backend", "s3-backend"

    # Implementation detail:
    - Replace the existing `registry` fixture with a parametrized version
    - The parametrized fixture accepts both tmp_path (for local) and the mock_s3 fixture (for S3)
    - Use indirect parametrization or conditional logic inside the fixture:
      ```python
      @pytest.fixture(params=["local", "s3"])
      def registry(request, tmp_path, mock_s3):
          if request.param == "local":
              return ModelRegistry(base_dir=str(tmp_path / "registry"))
          else:
              from ml.models.storage_backends import S3StorageBackend
              backend = S3StorageBackend(bucket="model-artifacts")
              return ModelRegistry(backend=backend)
      ```
    - mock_s3 fixture handles env setup/teardown automatically

    # Test adjustments needed:
    - Tests that check path.endswith("v1") need update:
      - Current assertion: assert p1.endswith("v1")
      - Now save_model returns logical key like "ridge_standard/v1" not a filesystem path
      - Update assertion to: assert p1.endswith("v1") — still valid since key ends with v1
      - Or use: assert "v1" in p1

    - Tests that read files via Path(path) / "metadata.json":
      - TestSaveModel.test_metadata_contents: reads metadata.json from disk
      - TestSaveModel.test_features_contents: reads features.json from disk
      - TestSaveWinner.test_save_winner_sets_rank: reads metadata.json
      - TestActivation.test_activate_model: reads metadata.json
      - TestActivation.test_deactivate_all: reads metadata.json for each entry
      - UPDATE these to use registry.load_model() or registry._backend.read_json() instead of raw filesystem reads
      - This ensures they work with both local and S3 backends

    - Tests that check Path(path).exists():
      - TestSaveModel.test_save_creates_files: checks pipeline.pkl, metadata.json, features.json exist
      - UPDATE to use registry._backend.exists() instead of Path.exists()

    # Tests that need NO changes (already backend-agnostic):
    - TestLoadModel.test_load_latest_version → uses registry.load_model()
    - TestLoadModel.test_load_specific_version → uses registry.load_model()
    - TestLoadModel.test_loaded_pipeline_predicts → uses loaded pipeline
    - TestLoadModel.test_load_nonexistent_raises → checks FileNotFoundError
    - TestListModels.* → uses registry.list_models()
    - TestGetWinner.* → uses registry.get_winner()
    - TestDeleteModel.* → uses registry.delete_model() + list_models()
    - TestActivation.test_activate_nonexistent_raises → checks FileNotFoundError
    - TestActivation.test_get_active_model → uses registry.get_active_model()
    - TestActivation.test_get_active_model_none → uses registry.get_active_model()
    - TestActivation.test_activate_then_deactivate → uses registry methods
    - TestActivation.test_activate_updates_only_target → uses registry methods

    # Total expected test count after parametrization:
    - Existing: ~25 test cases → run 2x (local + S3) = ~50 parametrized runs
    - New storage backend tests: ~20 test cases
    - New factory tests: ~5 test cases
    - Grand total: ~75 test functions

    # ━━━━━━━━━━ VERIFICATION ━━━━━━━━━━

    # Run command:
    - cd stock-prediction-platform && python -m pytest ml/tests/test_registry.py ml/tests/test_storage_backends.py -v
    - ALL tests must pass (both local and S3 parametrized variants)
    - No tests should be skipped or xfailed

  artifacts:
    - ml/tests/test_storage_backends.py (NEW — unit tests for Local + S3 backends + factory)
    - ml/tests/test_registry.py (MODIFIED — parametrized to run on both backends)
    - ml/tests/conftest.py (MODIFIED/CREATED — shared mock_s3 + s3_backend + s3_registry fixtures)
    - ml/requirements.txt (MODIFIED — add moto[s3] test dependency)
  key_links:
    - ml/models/storage_backends.py (plan 52-01 — the code under test)
    - ml/models/registry.py (plan 52-01 — refactored registry under test)
    - ml/tests/test_registry.py (existing tests — must continue to pass)
    - ml/tests/conftest.py (shared fixtures — may already exist)
---

<objective>
Add comprehensive tests for the new storage backend abstraction and update existing
ModelRegistry tests to run against both local filesystem and S3 (moto-mocked) backends.

Purpose: Validate OBJST-06 (save/load via S3 API), OBJST-07 (correct S3 path structure),
and OBJST-08 (STORAGE_BACKEND toggle) work correctly. Ensure zero regression in
existing ModelRegistry behavior.

Output: New test_storage_backends.py module, updated test_registry.py with dual-backend
parametrization, shared conftest.py fixtures with moto S3 mock, moto added as test dependency.
</objective>

<scope_exclusions>
- Implementation code changes (completed in plan 52-01)
- Integration tests against live MinIO cluster
- Training/drift pipeline tests (phase 53)
- Performance benchmarking
- API service tests (services/api/)
</scope_exclusions>

<execution_context>
@~/.Copilot/skills/execute-plan/SKILL.md
@~/.Copilot/get-stuff-done/templates/summary.md
</execution_context>
