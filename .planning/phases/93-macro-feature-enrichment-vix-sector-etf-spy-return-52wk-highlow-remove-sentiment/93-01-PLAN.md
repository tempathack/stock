---
phase: 93-macro-feature-enrichment-vix-sector-etf-spy-return-52wk-highlow-remove-sentiment
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - stock-prediction-platform/ml/tests/test_feast_store.py
  - stock-prediction-platform/ml/tests/test_data_loader.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "test_feast_store.py has test asserting sentiment columns are NOT in _TRAINING_FEATURES"
    - "test_feast_store.py has test asserting VIX, SPY, sector ETF, 52wk features ARE in _TRAINING_FEATURES"
    - "test_data_loader.py has failing test for load_yfinance_macro() function (ImportError RED)"
    - "All new tests fail in RED state — implementations not yet written"
  artifacts:
    - path: "stock-prediction-platform/ml/tests/test_feast_store.py"
      provides: "Sentiment removal + yfinance macro feature coverage assertions"
      contains: "test_sentiment_removed_from_training_features"
    - path: "stock-prediction-platform/ml/tests/test_data_loader.py"
      provides: "Failing test stub for yfinance macro loader"
      contains: "test_load_yfinance_macro_returns_dataframe"
---

<objective>
Write RED-state TDD tests that define the behavioral contracts for Phase 93 changes.
Tests must fail NOW (before implementation) and pass after execution plans 02 and 03 complete.

Contracts to encode:
1. Sentiment features are gone from _TRAINING_FEATURES
2. Four yfinance macro features exist in _TRAINING_FEATURES: vix, spy_return, sector_return, high52w_pct, low52w_pct
3. load_yfinance_macro() function exists and returns a DataFrame with date index and the 4 feature columns
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tempa/Desktop/priv-project/.planning/ROADMAP.md

<interfaces>
From ml/features/feast_store.py (current state):
```python
_TRAINING_FEATURES: list[str]  # currently contains reddit_sentiment_fv:* columns
```

From ml/pipelines/components/data_loader.py:
```python
# load_yfinance_macro() does NOT yet exist — test will ImportError/AttributeError
```

Sentinel: Four yfinance macro columns to add to training:
- yfinance_macro_fv:vix
- yfinance_macro_fv:spy_return
- yfinance_macro_fv:sector_return  (sector ETF return for the ticker's GICS sector)
- yfinance_macro_fv:high52w_pct    (pct above 52-week low)
- yfinance_macro_fv:low52w_pct     (pct below 52-week high)

Sentiment columns to assert ABSENT:
- reddit_sentiment_fv:avg_sentiment
- reddit_sentiment_fv:mention_count
- reddit_sentiment_fv:positive_ratio
- reddit_sentiment_fv:negative_ratio
</interfaces>
</context>

<tasks>
1. Open ml/tests/test_feast_store.py
   Add two test methods to the existing test class (or create the class if missing):
   - test_sentiment_removed_from_training_features: assert none of the 4 sentiment columns are in _TRAINING_FEATURES
   - test_yfinance_macro_features_present: assert all 5 yfinance_macro_fv columns are in _TRAINING_FEATURES

2. Open ml/tests/test_data_loader.py
   Add TestYfinanceMacroLoader class with one failing test:
   - test_load_yfinance_macro_returns_dataframe: imports load_yfinance_macro from data_loader, calls it with tickers=["AAPL"], start_date, end_date, asserts result is a DataFrame with columns [vix, spy_return, sector_return, high52w_pct, low52w_pct] and a DatetimeIndex

3. Run pytest on both files, confirm the new tests FAIL (RED), existing tests still pass
</tasks>
