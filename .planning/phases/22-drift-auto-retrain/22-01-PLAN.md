# Plan 22-01 — Prediction Generator + Drift-to-Retrain Integration + Tests

## Objective
Wire prediction generation into the drift auto-retrain flow and add integration tests. Satisfies **DRIFT-06** (auto-trigger on drift — already working) and **DRIFT-07** (predictions regenerated post-retrain — remaining work).

## Dependencies
- `ml/drift/trigger.py` — `evaluate_and_trigger()` (Phase 21) ✅
- `ml/pipelines/drift_pipeline.py` — `trigger_retraining()` (Phase 20) ✅
- `ml/pipelines/components/deployer.py` — serving directory structure (Phase 19) ✅
- `ml/pipelines/components/predictor.py` — `generate_predictions()` + `save_predictions()` ✅
- `ml/tests/test_predictor.py` — prediction unit tests ✅

## Pre-existing Work (Done in Earlier Phases)
- `ml/pipelines/components/predictor.py` — fully implemented (generate_predictions, save_predictions)
- `ml/tests/test_predictor.py` — 7 tests (TestGeneratePredictions: 5, TestSavePredictions: 2)
- `evaluate_and_trigger()` already calls `trigger_retraining()` on drift ✅

## Remaining Work
1. Wire `generate_predictions()` + `save_predictions()` into `evaluate_and_trigger()` after successful retrain
2. Add `regenerate_predictions` flag to `evaluate_and_trigger()` (default True)
3. Export `generate_predictions` and `save_predictions` from `ml/pipelines/components/__init__.py`
4. Add integration test: drift detection → retrain → predict cycle
5. Add trigger tests covering the new prediction-regeneration path

## Files to Create / Update

<tasks>

<task type="auto">
  <name>Task 1: Wire predictor into drift trigger + update exports + tests</name>

  <files>
    <file path="ml/drift/trigger.py" action="update">
      Add `regenerate_predictions` parameter (default True) to `evaluate_and_trigger()`.
      After successful retrain, call `generate_predictions()` + `save_predictions()` when
      `regenerate_predictions=True` and `data_dict` is available.
    </file>

    <file path="ml/pipelines/components/__init__.py" action="update">
      Add `generate_predictions` and `save_predictions` to imports and `__all__`.
    </file>

    <file path="ml/tests/test_trigger.py" action="update">
      Add tests:
      - `test_drift_triggers_retrain_and_predictions` — verify predictions generated after retrain
      - `test_regenerate_predictions_false_skips` — verify predictions skipped when flag is False
    </file>

    <file path="ml/tests/test_drift_retrain_integration.py" action="create">
      End-to-end integration test:
      - `test_drift_retrain_predict_cycle` — synthetic drift → retrain → predictions generated
      Uses mocked training pipeline but real predictor with tmp_path serving dir.
    </file>
  </files>

  <verify>
    <automated>cd stock\stock-prediction-platform; python -m pytest ml/tests/test_trigger.py ml/tests/test_predictor.py ml/tests/test_drift_retrain_integration.py -v --tb=short --rootdir=ml/tests</automated>
    <expected>All tests pass</expected>
  </verify>
</task>

</tasks>
