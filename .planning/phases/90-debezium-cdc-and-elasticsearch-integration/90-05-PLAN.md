---
phase: 90-debezium-cdc-and-elasticsearch-integration
plan: 05
type: execute
wave: 4
depends_on:
  - 90-04
files_modified: []
autonomous: false
requirements:
  - TBD
must_haves:
  truths:
    - "The /search page loads in the browser and shows the Search heading"
    - "The unified search input is visible and autofocused"
    - "All four tabs (Predictions, Models, Drift Events, Stocks) render"
    - "The Search nav item appears in the sidebar"
    - "No JavaScript console errors on page load"
  artifacts: []
  key_links: []
---

<objective>
Verify the Search page renders correctly in the live browser using Playwright MCP. Confirm page structure, navigation, search input, tabs, and empty states match the UI spec. This is the final gate for Phase 90 frontend work.

Purpose: CLAUDE.md requires Playwright verification for all frontend-verifiable implementations before declaring done.
Output: Screenshots confirming Search page renders correctly.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Playwright browser verification of /search page</name>
  <read_first>
    - .planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-UI-SPEC.md (exact copy to verify against)
  </read_first>
  <what-built>
The React /search page with:
- Container + PageHeader (title="Search", subtitle="Search predictions, models, drift events, and stocks")
- Unified search TextField with autoFocus, SearchIcon adornment, 300ms debounce
- MUI Tabs (variant="scrollable"): Predictions / Models / Drift Events / Stocks
- Empty state "Start searching" when no query is entered
- DataGrids with entity-specific columns when search results return
- Search nav item with SearchIcon in the sidebar
- Lazy-loaded /search route in App.tsx
  </what-built>
  <how-to-verify>
1. Start the frontend dev server if not running:
   ```bash
   cd stock-prediction-platform/services/frontend && npm run dev
   ```

2. Use Playwright MCP to navigate and verify:
   ```
   mcp__playwright__browser_navigate to http://localhost:5173/search (or http://localhost:3000/search)
   mcp__playwright__browser_take_screenshot
   ```

3. Verify these items in the screenshot:
   - [ ] Page heading reads "Search"
   - [ ] Subtitle reads "Search predictions, models, drift events, and stocks"
   - [ ] Search input box is visible (may be autofocused)
   - [ ] Four tabs visible: Predictions, Models, Drift Events, Stocks
   - [ ] Empty state shows "Start searching" text in the first tab
   - [ ] Search icon visible in sidebar navigation

4. Type in the search box and take another screenshot:
   ```
   mcp__playwright__browser_type text "AAPL" in the search input
   mcp__playwright__browser_take_screenshot
   ```

5. Verify:
   - [ ] Tab labels update to show counts or "Searching..." text
   - [ ] DataGrid or empty-results state appears after debounce (300ms)
   - [ ] No JavaScript errors in console

6. Click "Drift Events" tab and verify it renders.

7. Check sidebar: Search nav item with SearchIcon appears between Analytics and end of nav.
  </how-to-verify>
  <acceptance_criteria>
    - Screenshot shows "Search" heading and subtitle
    - Screenshot shows 4 tabs: Predictions, Models, Drift Events, Stocks
    - "Start searching" empty state visible before any input
    - Search nav item with SearchIcon visible in sidebar
    - No JavaScript errors visible or reported in Playwright console output
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Playwright MCP verification is the primary check for this plan. All assertions above must pass before declaring Phase 90 frontend complete.
</verification>

<success_criteria>
Playwright MCP confirms:
- /search page renders with correct heading, subtitle, 4 tabs, search input
- Empty state "Start searching" displays before query
- Search nav item visible in sidebar
- No console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-05-SUMMARY.md`
</output>
