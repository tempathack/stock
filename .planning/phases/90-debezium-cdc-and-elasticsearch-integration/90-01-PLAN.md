---
phase: 90-debezium-cdc-and-elasticsearch-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
  - stock-prediction-platform/k8s/storage/elasticsearch-pvc.yaml
  - stock-prediction-platform/k8s/storage/kibana-deployment.yaml
  - stock-prediction-platform/k8s/storage/postgresql-deployment.yaml
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Elasticsearch 8 StatefulSet exists in storage namespace with xpack.security.enabled=false and discovery.type=single-node"
    - "Kibana Deployment exists in storage namespace pointing to Elasticsearch service"
    - "PostgreSQL Deployment has args enabling wal_level=logical, max_replication_slots=5, max_wal_senders=5"
    - "10Gi PVC exists for Elasticsearch data persistence"
  artifacts:
    - path: "stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml"
      provides: "Elasticsearch 8 StatefulSet + ClusterIP Service (port 9200)"
      contains: "kind: StatefulSet"
    - path: "stock-prediction-platform/k8s/storage/elasticsearch-pvc.yaml"
      provides: "10Gi PVC for Elasticsearch"
      contains: "storage: 10Gi"
    - path: "stock-prediction-platform/k8s/storage/kibana-deployment.yaml"
      provides: "Kibana Deployment + NodePort Service"
      contains: "kind: Deployment"
    - path: "stock-prediction-platform/k8s/storage/postgresql-deployment.yaml"
      provides: "Updated PostgreSQL Deployment with WAL args"
      contains: "wal_level=logical"
  key_links:
    - from: "elasticsearch-statefulset.yaml"
      to: "elasticsearch-pvc.yaml"
      via: "volumeClaimTemplates referencing elasticsearch-pvc storage class"
    - from: "kibana-deployment.yaml"
      to: "elasticsearch service"
      via: "ELASTICSEARCH_HOSTS=http://elasticsearch.storage.svc.cluster.local:9200"
    - from: "postgresql-deployment.yaml"
      to: "Debezium connector"
      via: "args: wal_level=logical enables pgoutput plugin"
---

<objective>
Deploy Elasticsearch 8 and Kibana as plain K8s StatefulSet/Deployment in the storage namespace, following the existing redis/postgresql manifest patterns. Also patch the PostgreSQL Deployment to enable logical replication (required for Debezium CDC).

Purpose: Provides the search index store (Elasticsearch) and ops visibility tool (Kibana). Enables WAL-based CDC capture by setting wal_level=logical in PostgreSQL — without this, the Debezium connector fails at startup.
Output: Four YAML manifests (ES StatefulSet, ES PVC, Kibana Deployment, updated PostgreSQL Deployment with WAL args).
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-CONTEXT.md
@.planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-RESEARCH.md

<interfaces>
<!-- Existing patterns to replicate exactly -->

From k8s/storage/redis-deployment.yaml (template for ES StatefulSet + Service):
```yaml
# Pattern: Deployment + Service in same file, namespace: storage
# Labels: app: redis / app.kubernetes.io/part-of: stock-prediction-platform
# Resources: requests + limits
# Probes: livenessProbe + readinessProbe
# Service: ClusterIP on named port
```

From k8s/storage/postgresql-deployment.yaml (template for PVC volumeMount + WAL args location):
```yaml
# Pattern: args array on container spec for PostgreSQL CLI flags
# PVC referenced via volumes[].persistentVolumeClaim.claimName
# Service: ClusterIP, namespace: storage
# Existing container spec has no args[] — add args[] to inject WAL params
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Elasticsearch StatefulSet + PVC manifests</name>
  <files>
    stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
    stock-prediction-platform/k8s/storage/elasticsearch-pvc.yaml
  </files>
  <read_first>
    - stock-prediction-platform/k8s/storage/redis-deployment.yaml (Service + Deployment pattern, labels, resources)
    - stock-prediction-platform/k8s/storage/postgresql-deployment.yaml (PVC volume mount pattern)
    - stock-prediction-platform/k8s/storage/postgresql-pvc.yaml (PVC spec pattern)
  </read_first>
  <action>
Create `k8s/storage/elasticsearch-pvc.yaml`:
```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elasticsearch-pvc
  namespace: storage
  labels:
    app: elasticsearch
    app.kubernetes.io/part-of: stock-prediction-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
```

Create `k8s/storage/elasticsearch-statefulset.yaml` as a single file with StatefulSet + Service (two YAML documents separated by `---`):

StatefulSet:
- `apiVersion: apps/v1`, `kind: StatefulSet`
- `name: elasticsearch`, `namespace: storage`
- `labels: app: elasticsearch, app.kubernetes.io/part-of: stock-prediction-platform`
- `spec.replicas: 1`
- `spec.selector.matchLabels: app: elasticsearch`
- `spec.serviceName: elasticsearch`
- Container `name: elasticsearch`, `image: docker.elastic.co/elasticsearch/elasticsearch:8.13.4`
- Container ports: `9200` (name: http), `9300` (name: transport)
- Container env vars (MUST be exact names — ES reads these as settings):
  ```
  - name: discovery.type
    value: single-node
  - name: xpack.security.enabled
    value: "false"
  - name: ES_JAVA_OPTS
    value: "-Xms512m -Xmx512m"
  - name: cluster.name
    value: stock-platform-es
  ```
- Init container to fix permissions:
  ```yaml
  initContainers:
    - name: fix-permissions
      image: busybox
      command: ["sh", "-c", "chown -R 1000:1000 /usr/share/elasticsearch/data"]
      volumeMounts:
        - name: elasticsearch-data
          mountPath: /usr/share/elasticsearch/data
      securityContext:
        runAsUser: 0
  ```
- Volume mount: `name: elasticsearch-data`, `mountPath: /usr/share/elasticsearch/data`
- Resources: `requests: cpu: "500m", memory: "1Gi"`, `limits: cpu: "2000m", memory: "2Gi"`
- livenessProbe: `httpGet path: /_cluster/health, port: 9200`, `initialDelaySeconds: 60`, `periodSeconds: 15`, `timeoutSeconds: 10`
- readinessProbe: `httpGet path: /_cluster/health?wait_for_status=yellow, port: 9200`, `initialDelaySeconds: 30`, `periodSeconds: 10`, `timeoutSeconds: 10`
- volumes (NOT volumeClaimTemplates since using external PVC):
  ```yaml
  volumes:
    - name: elasticsearch-data
      persistentVolumeClaim:
        claimName: elasticsearch-pvc
  ```

Service (second document, after `---`):
- `kind: Service`, `name: elasticsearch`, `namespace: storage`
- `labels: app: elasticsearch`
- `spec.selector: app: elasticsearch`
- `spec.type: ClusterIP`
- ports: `- port: 9200, targetPort: 9200, name: http` and `- port: 9300, targetPort: 9300, name: transport`
  </action>
  <verify>
    <automated>grep -c "kind:" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml | grep -q "^2$" && grep "storage: 10Gi" stock-prediction-platform/k8s/storage/elasticsearch-pvc.yaml && grep "xpack.security.enabled" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml && grep "discovery.type" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml && echo "PASS"</automated>
  </verify>
  <done>elasticsearch-statefulset.yaml contains StatefulSet + Service (2 YAML docs), elasticsearch-pvc.yaml requests 10Gi, both env vars `discovery.type=single-node` and `xpack.security.enabled=false` present, init container sets permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Create Kibana Deployment manifest + patch PostgreSQL WAL config</name>
  <files>
    stock-prediction-platform/k8s/storage/kibana-deployment.yaml
    stock-prediction-platform/k8s/storage/postgresql-deployment.yaml
  </files>
  <read_first>
    - stock-prediction-platform/k8s/storage/redis-deployment.yaml (Deployment + Service pattern to replicate)
    - stock-prediction-platform/k8s/storage/postgresql-deployment.yaml (READ FULL FILE before modifying — must see existing container spec to add args safely)
  </read_first>
  <action>
**Part A — Create `k8s/storage/kibana-deployment.yaml`:**

Two YAML documents in one file (Deployment + Service):

Deployment:
- `kind: Deployment`, `name: kibana`, `namespace: storage`
- `labels: app: kibana, app.kubernetes.io/part-of: stock-prediction-platform`
- `spec.replicas: 1`
- Container `name: kibana`, `image: docker.elastic.co/kibana/kibana:8.13.4` (MUST match ES version exactly)
- Container port: `5601`
- Env vars:
  ```yaml
  env:
    - name: ELASTICSEARCH_HOSTS
      value: "http://elasticsearch.storage.svc.cluster.local:9200"
    - name: SERVER_NAME
      value: "kibana"
    - name: KIBANA_SYSTEM_PASSWORD
      value: ""
  ```
- Resources: `requests: cpu: "200m", memory: "512Mi"`, `limits: cpu: "1000m", memory: "1Gi"`
- readinessProbe: `httpGet path: /api/status, port: 5601`, `initialDelaySeconds: 60`, `periodSeconds: 15`, `timeoutSeconds: 10`, `failureThreshold: 5`
- livenessProbe: `httpGet path: /api/status, port: 5601`, `initialDelaySeconds: 120`, `periodSeconds: 20`, `timeoutSeconds: 10`

Service (second document):
- `kind: Service`, `name: kibana`, `namespace: storage`
- `spec.type: NodePort`
- `ports: - port: 5601, targetPort: 5601, nodePort: 30601`
- `spec.selector: app: kibana`

**Part B — Patch `k8s/storage/postgresql-deployment.yaml`:**

Read the existing file first. Locate the PostgreSQL container spec. Add `args` array to the container (after `image:` line, before `ports:`):

```yaml
args:
  - "-c"
  - "wal_level=logical"
  - "-c"
  - "max_replication_slots=5"
  - "-c"
  - "max_wal_senders=5"
```

Do NOT change any other part of the file — only add the `args` array. If `args` already exists, append these entries. This requires a PostgreSQL pod restart after apply to take effect (documented in RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>grep "ELASTICSEARCH_HOSTS" stock-prediction-platform/k8s/storage/kibana-deployment.yaml && grep "nodePort: 30601" stock-prediction-platform/k8s/storage/kibana-deployment.yaml && grep "wal_level=logical" stock-prediction-platform/k8s/storage/postgresql-deployment.yaml && grep "max_replication_slots=5" stock-prediction-platform/k8s/storage/postgresql-deployment.yaml && grep "max_wal_senders=5" stock-prediction-platform/k8s/storage/postgresql-deployment.yaml && echo "PASS"</automated>
  </verify>
  <done>kibana-deployment.yaml has NodePort 30601 and ELASTICSEARCH_HOSTS env pointing to elasticsearch.storage.svc.cluster.local:9200. postgresql-deployment.yaml has args block with wal_level=logical, max_replication_slots=5, max_wal_senders=5.</done>
</task>

</tasks>

<verification>
Run the following checks on all four files:
```bash
# ES StatefulSet: required fields
grep "kind: StatefulSet" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "namespace: storage" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "discovery.type" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "xpack.security.enabled" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "ES_JAVA_OPTS" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "elasticsearch-pvc" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml
grep "8.13.4" stock-prediction-platform/k8s/storage/elasticsearch-statefulset.yaml

# ES PVC
grep "storage: 10Gi" stock-prediction-platform/k8s/storage/elasticsearch-pvc.yaml

# Kibana
grep "8.13.4" stock-prediction-platform/k8s/storage/kibana-deployment.yaml
grep "nodePort: 30601" stock-prediction-platform/k8s/storage/kibana-deployment.yaml
grep "elasticsearch.storage.svc.cluster.local" stock-prediction-platform/k8s/storage/kibana-deployment.yaml

# PostgreSQL WAL
grep "wal_level=logical" stock-prediction-platform/k8s/storage/postgresql-deployment.yaml
```
</verification>

<success_criteria>
- elasticsearch-statefulset.yaml: StatefulSet (image 8.13.4) + Service in storage namespace, init container, correct env vars, references elasticsearch-pvc
- elasticsearch-pvc.yaml: 10Gi ReadWriteOnce in storage namespace
- kibana-deployment.yaml: Kibana 8.13.4 Deployment + NodePort 30601, ELASTICSEARCH_HOSTS points to ES ClusterIP service
- postgresql-deployment.yaml: args[] added with wal_level=logical, max_replication_slots=5, max_wal_senders=5 — no other lines changed
</success_criteria>

<output>
After completion, create `.planning/phases/90-debezium-cdc-and-elasticsearch-integration/90-01-SUMMARY.md`
</output>
