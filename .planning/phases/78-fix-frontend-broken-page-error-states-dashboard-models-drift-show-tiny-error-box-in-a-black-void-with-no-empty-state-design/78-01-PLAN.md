---
phase: 78-fix-frontend-broken-page-error-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx
autonomous: true
requirements:
  - ERR-FALLBACK-ICON
must_haves:
  truths:
    - "ErrorFallback renders an ErrorOutline icon above the message text"
    - "ErrorFallback centers its content vertically and horizontally"
    - "All existing callers (Models, Dashboard, Drift, Forecasts) automatically get the improved layout with no code changes on their side"
  artifacts:
    - path: "stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx"
      provides: "Enhanced error fallback with icon + centered layout"
      contains: "ErrorOutlineIcon"
  key_links:
    - from: "ErrorFallback.tsx"
      to: "ErrorOutlineIcon"
      via: "import ErrorOutlineIcon from '@mui/icons-material/ErrorOutline'"
      pattern: "ErrorOutlineIcon"
---

<objective>
Enhance the shared `ErrorFallback` component in-place: add an `ErrorOutline` MUI icon above the message text.

Purpose: All page-level and panel-level error states across Models, Dashboard, Drift, and Forecasts automatically use the improved design without any changes to those pages. This is the prerequisite for Plans 02-04.

Output: Updated `ErrorFallback.tsx` with icon, message, optional Retry button — vertically stacked and centered.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-fix-frontend-broken-page-error-states-dashboard-models-drift-show-tiny-error-box-in-a-black-void-with-no-empty-state-design/78-CONTEXT.md

<interfaces>
<!-- Current ErrorFallback implementation — executor modifies this file directly -->

From stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx (current):
```typescript
import { Box, Button, Typography } from "@mui/material";

interface ErrorFallbackProps {
  message?: string;
  onRetry?: () => void;
}

export default function ErrorFallback({
  message = "Something went wrong",
  onRetry,
}: ErrorFallbackProps) {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        border: "1px solid",
        borderColor: "divider",
        borderRadius: 1,
        p: 4,
      }}
    >
      <Typography color="error.main" variant="body1" fontWeight={500}>
        {message}
      </Typography>
      {onRetry && (
        <Button variant="contained" onClick={onRetry} sx={{ mt: 2 }}>
          Retry
        </Button>
      )}
    </Box>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ErrorOutline icon to ErrorFallback component</name>
  <files>stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx</files>
  <read_first>
    - stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx
  </read_first>
  <action>
Rewrite `ErrorFallback.tsx` to add an `ErrorOutlineIcon` above the message text. The component interface (`message`, `onRetry`) does not change — all existing callers are unaffected.

Exact implementation:

```typescript
import ErrorOutlineIcon from "@mui/icons-material/ErrorOutline";
import { Box, Button, Typography } from "@mui/material";

interface ErrorFallbackProps {
  message?: string;
  onRetry?: () => void;
}

export default function ErrorFallback({
  message = "Something went wrong",
  onRetry,
}: ErrorFallbackProps) {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        border: "1px solid",
        borderColor: "divider",
        borderRadius: 1,
        p: 4,
        gap: 1,
      }}
    >
      <ErrorOutlineIcon sx={{ color: "error.main", fontSize: 40 }} />
      <Typography color="error.main" variant="body1" fontWeight={500}>
        {message}
      </Typography>
      {onRetry && (
        <Button variant="contained" onClick={onRetry} sx={{ mt: 1 }}>
          Retry
        </Button>
      )}
    </Box>
  );
}
```

Note: `@mui/icons-material` is already a dependency in this project (used by Dashboard.tsx and other pages). No new package install needed.
  </action>
  <verify>
    <automated>grep -n "ErrorOutlineIcon" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/frontend/src/components/ui/ErrorFallback.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ErrorOutlineIcon" ErrorFallback.tsx` returns at least 2 lines (import + JSX usage)
    - `grep "from \"@mui/icons-material/ErrorOutline\"" ErrorFallback.tsx` returns 1 line
    - `grep "interface ErrorFallbackProps" ErrorFallback.tsx` returns 1 line (interface unchanged)
    - `grep "onRetry" ErrorFallback.tsx` returns lines including the Button conditional (prop preserved)
    - `cd stock-prediction-platform/services/frontend && npx tsc --noEmit 2>&1 | grep ErrorFallback` returns no errors
  </acceptance_criteria>
  <done>ErrorFallback.tsx exports the same interface as before, renders ErrorOutlineIcon above the Typography message, TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
```bash
cd /home/tempa/Desktop/priv-project/stock-prediction-platform/services/frontend
npx tsc --noEmit 2>&1 | head -20
```
TypeScript compilation must report zero errors in ErrorFallback.tsx.
</verification>

<success_criteria>
- ErrorFallback.tsx imports and renders `ErrorOutlineIcon` from `@mui/icons-material/ErrorOutline`
- Component props interface `{ message?, onRetry? }` is unchanged
- TypeScript compiles clean
- All existing callers (Models, Dashboard, Drift, Forecasts) need zero changes to get the improved design
</success_criteria>

<output>
After completion, create `.planning/phases/78-fix-frontend-broken-page-error-states-dashboard-models-drift-show-tiny-error-box-in-a-black-void-with-no-empty-state-design/78-01-SUMMARY.md`
</output>
