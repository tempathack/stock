---
plan: "01-02"
phase: 1
wave: 1
depends_on: []
requirements_addressed: [INFRA-06]
files_modified:
  - stock-prediction-platform/docker-compose.yml
autonomous: true
---

## Objective

Create or replace `docker-compose.yml` with stub service definitions for all 6 services: `api`, `kafka-consumer`, `postgres`, `kafka`, `zookeeper`, `frontend`. Each service has only `image:` and `ports:` (where applicable) — no volumes, no environment, no depends_on, no networks. This file serves as the service inventory for later phases to flesh out.

## Tasks

<task id="1">
  <title>Write docker-compose.yml with all 6 service stubs</title>
  <read_first>
    - stock-prediction-platform/docker-compose.yml (current state — file may already exist with content from prior work)
    - .planning/phases/01-repo-folder-scaffold/01-CONTEXT.md (decisions section — stub-only, no volumes/env_file/depends_on)
    - .planning/phases/01-repo-folder-scaffold/01-RESEARCH.md (Section 2 — docker-compose stub patterns, service inventory table)
    - Project_scope.md (Section 15 — docker-compose.yml listed at root)
  </read_first>
  <action>
Overwrite `stock-prediction-platform/docker-compose.yml` with the following exact content. Omit the `version:` key (Compose V2 best practice per research). Use the exact service names, image names, and port mappings from the research:

```yaml
services:
  api:
    image: stock-prediction-api:latest
    ports:
      - "8000:8000"

  kafka-consumer:
    image: stock-prediction-consumer:latest

  postgres:
    image: timescale/timescaledb:latest-pg15
    ports:
      - "5432:5432"

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    ports:
      - "9092:9092"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    ports:
      - "2181:2181"

  frontend:
    image: stock-prediction-frontend:latest
    ports:
      - "3000:3000"
```

Key points:
- `kafka-consumer` has NO ports section (it is an internal consumer, not exposed)
- No `volumes:`, `environment:`, `env_file:`, `depends_on:`, `networks:`, `restart:`, or `healthcheck:` on any service
- No top-level `volumes:` or `networks:` blocks
  </action>
  <acceptance_criteria>
    - `test -f stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "^services:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  api:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  kafka-consumer:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  postgres:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  kafka:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  zookeeper:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "  frontend:" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "stock-prediction-api:latest" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "stock-prediction-consumer:latest" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "timescale/timescaledb:latest-pg15" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "confluentinc/cp-kafka:7.5.0" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "confluentinc/cp-zookeeper:7.5.0" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q "stock-prediction-frontend:latest" stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q '"8000:8000"' stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q '"5432:5432"' stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q '"9092:9092"' stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q '"2181:2181"' stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -q '"3000:3000"' stock-prediction-platform/docker-compose.yml` returns exit code 0
    - `grep -c "volumes:" stock-prediction-platform/docker-compose.yml` returns 0 (no volumes anywhere)
    - `grep -c "depends_on:" stock-prediction-platform/docker-compose.yml` returns 0 (no depends_on anywhere)
    - `grep -c "environment:" stock-prediction-platform/docker-compose.yml` returns 0 (no environment anywhere)
    - `grep -c "env_file:" stock-prediction-platform/docker-compose.yml` returns 0 (no env_file anywhere)
    - File does NOT contain the string `version:` (Compose V2 best practice)
  </acceptance_criteria>
</task>

## Verification

```bash
cd stock-prediction-platform && \
grep -q "^services:" docker-compose.yml && \
grep -q "  api:" docker-compose.yml && \
grep -q "  kafka-consumer:" docker-compose.yml && \
grep -q "  postgres:" docker-compose.yml && \
grep -q "  kafka:" docker-compose.yml && \
grep -q "  zookeeper:" docker-compose.yml && \
grep -q "  frontend:" docker-compose.yml && \
! grep -q "volumes:" docker-compose.yml && \
! grep -q "depends_on:" docker-compose.yml && \
! grep -q "environment:" docker-compose.yml && \
echo "DOCKER-COMPOSE VERIFIED"
```

## Must Haves

- [ ] `docker-compose.yml` exists at `stock-prediction-platform/docker-compose.yml`
- [ ] All 6 services present: `api`, `kafka-consumer`, `postgres`, `kafka`, `zookeeper`, `frontend`
- [ ] Each service has the correct image name as specified in the research
- [ ] Port mappings: api=8000, postgres=5432, kafka=9092, zookeeper=2181, frontend=3000
- [ ] `kafka-consumer` has NO ports section
- [ ] No `volumes:`, `environment:`, `env_file:`, `depends_on:`, `networks:`, `restart:`, or `healthcheck:` directives present
- [ ] No `version:` key (Compose V2 best practice)
