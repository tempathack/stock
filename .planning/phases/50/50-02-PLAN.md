---
phase: 50-export-mobile-responsive
plan: 02
type: execute
wave: 4
depends_on: [50-01]
files_created:
  - services/frontend/src/components/dashboard/MobileMarketList.tsx
  - services/frontend/src/components/ui/ResponsiveTable.tsx
files_modified:
  - services/frontend/src/components/dashboard/MarketTreemap.tsx
  - services/frontend/src/components/tables/ModelComparisonTable.tsx
  - services/frontend/src/components/forecasts/ForecastTable.tsx
  - services/frontend/src/components/backtest/BacktestMetricsSummary.tsx
  - services/frontend/src/pages/Dashboard.tsx
  - services/frontend/src/pages/Backtest.tsx
  - services/frontend/src/components/layout/Layout.tsx
autonomous: true
scope_exclusions:
  - CSV/PDF export (completed in plan 50-01)
  - Full mobile-native rewrite (Tailwind responsive adjustments only)
  - Touch gestures (swipe, pinch-to-zoom) on charts
  - Mobile-specific navigation drawer animation polish beyond existing lg:hidden toggle
  - Chart responsiveness (Recharts ResponsiveContainer already handles this)
  - Print stylesheet (@media print)
  - Landscape vs portrait mode-specific handling
  - PWA manifest or mobile app shell
must_haves:
  truths:
    # ━━━━━━━━━━ TREEMAP → MOBILE LIST (FDASH on <640px) ━━━━━━━━━━

    # --- services/frontend/src/components/dashboard/MobileMarketList.tsx (NEW) ---
    - Component: MobileMarketList
    - Props: { data: TreemapSectorGroup[]; selectedTicker: string | null; onSelectTicker: (ticker: string) => void }
    - Renders a vertical stacked list of all stocks grouped by sector
    - Each sector has a bold header row with sector name
    - Each stock row shows: ticker (left, bold accent), company name (truncated), last close price, daily change % (color-coded green/red)
    - Selected ticker row gets border-l-2 border-accent bg-accent/10 highlight
    - Clicking a row calls onSelectTicker(ticker)
    - Styling: rounded-lg cards per sector, gap-2 between sectors, compact py-2 px-3 per stock row
    - No dependency on Recharts — pure Tailwind div/span layout

    # --- services/frontend/src/components/dashboard/MarketTreemap.tsx (MODIFIED) ---
    - Add a useMediaQuery hook or Tailwind-based responsive check:
      - On screens < 640px (sm breakpoint): render MobileMarketList instead of Treemap
      - On screens >= 640px: render existing Treemap as-is
    - Implementation: use a simple useState + window.matchMedia("(max-width: 639px)") listener
    - Import MobileMarketList from ./MobileMarketList
    - Pass same props (data, selectedTicker, onSelectTicker) to MobileMarketList

    # --- services/frontend/src/pages/Dashboard.tsx (MODIFIED) ---
    - No direct changes needed — MarketTreemap internally handles the mobile switch
    - Verify padding is appropriate on mobile: main container p-6 may reduce to p-4 on sm (optional, check layout)

    # ━━━━━━━━━━ TABLES → CARD LAYOUT (<640px) ━━━━━━━━━━

    # --- services/frontend/src/components/forecasts/ForecastTable.tsx (MODIFIED) ---
    - Add mobile card layout below sm breakpoint:
      - Wrap existing table in a `hidden sm:block` container
      - Add a new `sm:hidden` container rendering cards
    - Card layout per ForecastRow:
      - Top row: ticker (bold accent, left) + trend badge (right)
      - Second row: company name (truncated, text-text-secondary text-xs)
      - Third row: two-column grid showing:
        - Current Price: $xxx.xx
        - Predicted: $xxx.xx
        - Return %: +x.xx% (colored)
        - Confidence: x.xx
      - Bottom row: daily change badge
    - Each card: rounded-lg border border-border bg-bg-surface p-3 mb-2
    - Selected card: border-l-2 border-l-accent bg-accent/10
    - Clicking card calls onSelectTicker(row.ticker)
    - Checkbox for compare: shown as a small toggle in top-right of card
    - No horizontal scrolling at 375px viewport width

    # --- services/frontend/src/components/tables/ModelComparisonTable.tsx (MODIFIED) ---
    - Add mobile card layout below sm breakpoint:
      - Wrap existing table in a `hidden sm:block` container
      - Add a new `sm:hidden` container rendering cards
    - Card layout per ModelComparisonEntry:
      - Header: model_name (bold) + winner star (★) if is_winner + active dot
      - Scaler: scaler_variant text-xs text-text-secondary
      - Metrics grid (2-col): OOS RMSE, OOS MAE, OOS R², Dir. Accuracy
      - Each metric: label (text-xs text-text-secondary) + value (font-mono)
    - Winner card: border-l-4 border-l-accent bg-accent/5
    - Clicking card calls onSelectModel(model)
    - Filter input stays visible above cards (full-width on mobile)

    # --- services/frontend/src/components/backtest/BacktestMetricsSummary.tsx (MODIFIED) ---
    - Current grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
    - Already responsive (grid-cols-1 on mobile), but verify:
      - Cards don't overflow at 375px width
      - Text doesn't truncate — reduce font-size from text-2xl to text-xl on mobile if needed: text-xl sm:text-2xl

    # ━━━━━━━━━━ LAYOUT RESPONSIVE AUDIT ━━━━━━━━━━

    # --- services/frontend/src/components/layout/Layout.tsx (MODIFIED) ---
    - Reduce main padding on mobile: change `p-6` to `p-4 sm:p-6` on the <main> element
    - Everything else (sidebar toggle, mobile header) already exists and works correctly

    # --- services/frontend/src/pages/Backtest.tsx (MODIFIED) ---
    - Controls bar: existing `flex flex-wrap items-end gap-4` is already responsive
    - Verify: at 375px width, controls wrap properly (each input/select on its own line)
    - If needed: add `w-full sm:w-auto` to each control <div> to ensure full-width on mobile

    # ━━━━━━━━━━ GENERAL RESPONSIVE RULES ━━━━━━━━━━
    - No horizontal scrollbar at 375px viewport width on ANY page
    - All font sizes readable on mobile (minimum 12px effective)
    - Touch targets ≥ 44px height for interactive elements on mobile
    - Existing `overflow-x-auto` on desktop tables is fine (desktop users can scroll)
    - Mobile card layouts replace, not supplement, desktop table scroll

  artifacts:
    - services/frontend/src/components/dashboard/MobileMarketList.tsx (NEW — stacked list for mobile treemap replacement)
    - services/frontend/src/components/dashboard/MarketTreemap.tsx (MODIFIED — conditional mobile/desktop rendering)
    - services/frontend/src/components/forecasts/ForecastTable.tsx (MODIFIED — add sm:hidden card layout)
    - services/frontend/src/components/tables/ModelComparisonTable.tsx (MODIFIED — add sm:hidden card layout)
    - services/frontend/src/components/backtest/BacktestMetricsSummary.tsx (MODIFIED — responsive font check)
    - services/frontend/src/components/layout/Layout.tsx (MODIFIED — mobile padding adjustment)
    - services/frontend/src/pages/Dashboard.tsx (MODIFIED — verify mobile padding)
    - services/frontend/src/pages/Backtest.tsx (MODIFIED — full-width controls on mobile)
  key_links:
    - services/frontend/src/api/types.ts (TreemapSectorGroup, TreemapCell, ForecastRow, ModelComparisonEntry)
    - services/frontend/src/styles/globals.css (Tailwind theme variables)
    - services/frontend/src/components/layout/Sidebar.tsx (existing mobile nav reference)
    - services/frontend/src/components/dashboard/index.ts (barrel export — add MobileMarketList)
    - services/frontend/src/utils/dashboardUtils.ts (changePctToColor — reuse in MobileMarketList)
---

<objective>
Audit and fix the mobile responsive layout across all pages. The dashboard treemap
converts to a stacked sector-grouped list on screens < 640px. All data tables
(ForecastTable, ModelComparisonTable) convert to card layouts on mobile. No
horizontal scrollbar at 375px viewport width on any page.

Purpose: FENH-07 (Mobile responsive layout — sm/md/lg breakpoints, treemap→list,
tables→cards).
Output: MobileMarketList component, responsive conditional rendering in
MarketTreemap, mobile card layouts in ForecastTable + ModelComparisonTable,
layout padding adjustments.
</objective>

<scope_exclusions>
- CSV/PDF export (completed in plan 50-01)
- Mobile-native rewrite or PWA manifest
- Touch gestures or pinch-to-zoom on charts
- Print stylesheet
- Chart responsiveness beyond existing ResponsiveContainer (already handled)
- Landscape/portrait mode-specific handling
</scope_exclusions>

<execution_context>
@~/.Copilot/skills/execute-plan/SKILL.md
@~/.Copilot/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key references:
@services/frontend/src/components/dashboard/MarketTreemap.tsx (add responsive treemap→list switch)
@services/frontend/src/components/forecasts/ForecastTable.tsx (add mobile card layout)
@services/frontend/src/components/tables/ModelComparisonTable.tsx (add mobile card layout)
@services/frontend/src/components/backtest/BacktestMetricsSummary.tsx (verify responsive font)
@services/frontend/src/components/layout/Layout.tsx (mobile padding)
@services/frontend/src/pages/Dashboard.tsx (verify mobile)
@services/frontend/src/pages/Backtest.tsx (full-width controls on mobile)
@services/frontend/src/api/types.ts (TreemapSectorGroup, ForecastRow, ModelComparisonEntry)
@services/frontend/src/utils/dashboardUtils.ts (changePctToColor for MobileMarketList)
@services/frontend/src/styles/globals.css (Tailwind theme)
</context>

<tasks>
  <task id="1">
    <name>Task 1: Create MobileMarketList component</name>
    <files>services/frontend/src/components/dashboard/MobileMarketList.tsx</files>
    <details>
Create `services/frontend/src/components/dashboard/MobileMarketList.tsx`:

```typescript
import type { TreemapSectorGroup } from "@/api";
import { changePctToColor } from "@/utils/dashboardUtils";

interface MobileMarketListProps {
  data: TreemapSectorGroup[];
  selectedTicker: string | null;
  onSelectTicker: (ticker: string) => void;
}

export default function MobileMarketList({
  data,
  selectedTicker,
  onSelectTicker,
}: MobileMarketListProps) {
  if (data.length === 0) {
    return (
      <div className="rounded-lg border-2 border-dashed border-border bg-bg-surface p-8 text-center text-text-secondary">
        No market data available
      </div>
    );
  }

  return (
    <div className="space-y-3">
      {data.map((sector) => (
        <div
          key={sector.name}
          className="rounded-lg border border-border bg-bg-surface overflow-hidden"
        >
          <div className="bg-bg-card px-3 py-2">
            <span className="text-xs font-semibold uppercase tracking-wider text-text-secondary">
              {sector.name}
            </span>
          </div>
          <div className="divide-y divide-border/50">
            {sector.children.map((stock) => {
              const isSelected = selectedTicker === stock.ticker;
              const pct = stock.dailyChangePct;
              return (
                <button
                  key={stock.ticker}
                  onClick={() => onSelectTicker(stock.ticker)}
                  className={`flex w-full items-center justify-between px-3 py-2.5 text-left transition-colors hover:bg-bg-card/40 ${
                    isSelected ? "border-l-2 border-l-accent bg-accent/10" : ""
                  }`}
                >
                  <div className="min-w-0 flex-1">
                    <span className="font-semibold text-accent">
                      {stock.ticker}
                    </span>
                    <span className="ml-2 truncate text-xs text-text-secondary">
                      {stock.name}
                    </span>
                  </div>
                  <div className="flex items-center gap-3 text-right">
                    <span className="text-sm font-mono text-text-primary">
                      ${stock.lastClose.toFixed(2)}
                    </span>
                    <span
                      className="min-w-[60px] text-right text-xs font-semibold font-mono"
                      style={{ color: changePctToColor(pct) }}
                    >
                      {pct >= 0 ? "+" : ""}
                      {pct.toFixed(2)}%
                    </span>
                  </div>
                </button>
              );
            })}
          </div>
        </div>
      ))}
    </div>
  );
}
```

Add to `services/frontend/src/components/dashboard/index.ts` barrel:
```typescript
export { default as MobileMarketList } from "./MobileMarketList";
```
    </details>
  </task>

  <task id="2">
    <name>Task 2: Add responsive mobile/desktop switch to MarketTreemap</name>
    <files>services/frontend/src/components/dashboard/MarketTreemap.tsx</files>
    <details>
Modify `MarketTreemap.tsx` to conditionally render MobileMarketList on small
screens:

1. Add at the top of the file:
```typescript
import { useState, useEffect } from "react";
import MobileMarketList from "./MobileMarketList";
```

2. Add a `useIsMobile` check inside the component body (before the existing useMemo):
```typescript
const [isMobile, setIsMobile] = useState(false);

useEffect(() => {
  const mql = window.matchMedia("(max-width: 639px)");
  setIsMobile(mql.matches);
  const handler = (e: MediaQueryListEvent) => setIsMobile(e.matches);
  mql.addEventListener("change", handler);
  return () => mql.removeEventListener("change", handler);
}, []);
```

3. In the return, insert a mobile branch before the existing treemap JSX:
```typescript
if (isMobile) {
  return (
    <MobileMarketList
      data={data}
      selectedTicker={selectedTicker}
      onSelectTicker={onSelectTicker}
    />
  );
}
```

Keep all existing desktop treemap code unchanged below the mobile branch.
    </details>
  </task>

  <task id="3">
    <name>Task 3: Add mobile card layout to ForecastTable</name>
    <files>services/frontend/src/components/forecasts/ForecastTable.tsx</files>
    <details>
Modify `ForecastTable.tsx` to show cards on mobile:

1. Wrap the existing `<div className="overflow-x-auto ...">` with `<div className="hidden sm:block">`:
```tsx
{/* Desktop table */}
<div className="hidden sm:block">
  <div className="overflow-x-auto rounded-lg border border-border bg-bg-surface">
    <table ...>
      ...existing table...
    </table>
  </div>
</div>
```

2. Add a mobile card list before or after the desktop table:
```tsx
{/* Mobile cards */}
<div className="space-y-2 sm:hidden">
  {sorted.map((row) => {
    const isSelected = row.ticker === selectedTicker;
    const isCompare = comparisonTickers.includes(row.ticker);
    return (
      <div
        key={row.ticker}
        onClick={() => onSelectTicker(row.ticker)}
        className={`rounded-lg border border-border bg-bg-surface p-3 cursor-pointer transition-colors hover:bg-bg-card/40 ${
          isSelected ? "border-l-2 border-l-accent bg-accent/10" : ""
        } ${isCompare && !isSelected ? "border-l-2 border-l-accent/50" : ""}`}
      >
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-2">
            <input
              type="checkbox"
              checked={isCompare}
              onChange={(e) => { e.stopPropagation(); onToggleCompare(row.ticker); }}
              onClick={(e) => e.stopPropagation()}
              className="accent-accent"
            />
            <span className="font-semibold text-accent">{row.ticker}</span>
          </div>
          {trendBadge(row.trend)}
        </div>
        {row.company_name && (
          <p className="mt-0.5 truncate text-xs text-text-secondary">{row.company_name}</p>
        )}
        <div className="mt-2 grid grid-cols-2 gap-x-4 gap-y-1 text-xs">
          <div>
            <span className="text-text-secondary">Price: </span>
            <span className="font-mono">{fmt(row.current_price, 2, "$")}</span>
          </div>
          <div>
            <span className="text-text-secondary">Predicted: </span>
            <span className="font-mono">{fmt(row.predicted_price, 2, "$")}</span>
          </div>
          <div>
            <span className="text-text-secondary">Return: </span>
            <span className={`font-mono font-semibold ${pctClass(row.expected_return_pct)}`}>
              {row.expected_return_pct > 0 ? "+" : ""}{row.expected_return_pct.toFixed(2)}%
            </span>
          </div>
          <div>
            <span className="text-text-secondary">Conf: </span>
            <span className="font-mono">{fmt(row.confidence, 2)}</span>
          </div>
        </div>
      </div>
    );
  })}
</div>
```

3. If `rows.length === 0`, the existing empty-state div stays outside both containers.
    </details>
  </task>

  <task id="4">
    <name>Task 4: Add mobile card layout to ModelComparisonTable</name>
    <files>services/frontend/src/components/tables/ModelComparisonTable.tsx</files>
    <details>
Modify `ModelComparisonTable.tsx` to show cards on mobile:

1. Wrap existing table `<div className="overflow-x-auto ...">` with `<div className="hidden sm:block">`.

2. Add mobile cards before the desktop table:
```tsx
{/* Mobile cards */}
<div className="space-y-2 sm:hidden">
  {sorted.map((model) => (
    <div
      key={`${model.model_name}-${model.scaler_variant}-${model.version}`}
      onClick={() => onSelectModel?.(model)}
      className={`rounded-lg border border-border bg-bg-surface p-3 cursor-pointer transition-colors hover:bg-bg-card/40 ${
        model.is_winner ? "border-l-4 border-l-accent bg-accent/5" : ""
      }`}
    >
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          {model.is_winner && <span className="text-xs text-accent">★</span>}
          <span className="font-medium text-text-primary">{model.model_name}</span>
        </div>
        <span
          className={`inline-block h-2 w-2 rounded-full ${
            model.is_active ? "bg-profit" : "bg-text-secondary/40"
          }`}
        />
      </div>
      <p className="mt-0.5 text-xs text-text-secondary">{model.scaler_variant}</p>
      <div className="mt-2 grid grid-cols-2 gap-x-4 gap-y-1 text-xs">
        <div>
          <span className="text-text-secondary">RMSE: </span>
          <span className="font-mono">{fmt(model.oos_metrics.rmse, 6)}</span>
        </div>
        <div>
          <span className="text-text-secondary">MAE: </span>
          <span className="font-mono">{fmt(model.oos_metrics.mae, 6)}</span>
        </div>
        <div>
          <span className="text-text-secondary">R²: </span>
          <span className="font-mono">{fmt(model.oos_metrics.r2, 4)}</span>
        </div>
        <div>
          <span className="text-text-secondary">Dir. Acc: </span>
          <span className="font-mono">{pct(model.oos_metrics.directional_accuracy, 2)}</span>
        </div>
      </div>
    </div>
  ))}
</div>
```

3. Filter input remains outside both containers (visible on all screen sizes).
    </details>
  </task>

  <task id="5">
    <name>Task 5: Fix BacktestMetricsSummary responsive font</name>
    <files>services/frontend/src/components/backtest/BacktestMetricsSummary.tsx</files>
    <details>
Change the metric value font size from `text-2xl` to `text-xl sm:text-2xl`:

```tsx
<p className="text-xl sm:text-2xl font-bold" style={{ color: accent }}>
```

This ensures values don't overflow on narrow (375px) screens.
    </details>
  </task>

  <task id="6">
    <name>Task 6: Adjust Layout.tsx mobile padding</name>
    <files>services/frontend/src/components/layout/Layout.tsx</files>
    <details>
In Layout.tsx, change the `<main>` element padding from `p-6` to `p-4 sm:p-6`:

```tsx
<main className="flex-1 overflow-y-auto p-4 sm:p-6">
```

This gives more breathing room on narrow mobile screens.
    </details>
  </task>

  <task id="7">
    <name>Task 7: Make Backtest controls full-width on mobile</name>
    <files>services/frontend/src/pages/Backtest.tsx</files>
    <details>
In Backtest.tsx, add `w-full sm:w-auto` to each control wrapper `<div>` inside the
controls bar so they stack as full-width inputs on mobile:

```tsx
<div className="mb-6 flex flex-wrap items-end gap-4">
  <div className="w-full sm:w-auto">
    <label ...>Ticker</label>
    <select className="w-full rounded-md ..." ...>
  </div>
  <div className="w-full sm:w-auto">
    <label ...>Start Date</label>
    <input className="w-full rounded-md ..." ...>
  </div>
  <div className="w-full sm:w-auto">
    <label ...>End Date</label>
    <input className="w-full rounded-md ..." ...>
  </div>
  <div className="w-full sm:w-auto">
    <label ...>Horizon</label>
    <select className="w-full rounded-md ..." ...>
  </div>
  <button className="w-full sm:w-auto rounded-md bg-accent ..." ...>Run Backtest</button>
  <ExportButtons ... />
</div>
```

Also ensure select/input elements have `w-full` so they expand to fill the wrapper.
    </details>
  </task>

  <task id="8">
    <name>Task 8: Build verification + 375px check</name>
    <files>services/frontend/</files>
    <details>
1. Run `npx tsc --noEmit` — verify no TypeScript errors.
2. Run `npm run build` — verify production build succeeds.
3. Open dev server (`npm run dev`), use Chrome DevTools responsive mode at 375px width.
4. Verify each page:
   - Dashboard: treemap replaced with stacked list, no horizontal scroll
   - Forecasts: table replaced with cards, no horizontal scroll
   - Models: table replaced with cards, no horizontal scroll
   - Backtest: controls stack vertically, metrics fit, no horizontal scroll
   - Drift: existing layout works (no changes needed — already card-based)
5. Verify at 1440px: no regressions, treemap and tables render normally.
    </details>
  </task>
</tasks>
