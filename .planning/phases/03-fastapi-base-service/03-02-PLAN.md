---
phase: 03-fastapi-base-service
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - stock-prediction-platform/services/api/Dockerfile
  - stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml
  - stock-prediction-platform/k8s/ingestion/fastapi-service.yaml
  - stock-prediction-platform/scripts/deploy-all.sh
autonomous: false
requirements:
  - API-03
  - API-04

must_haves:
  truths:
    - "Dockerfile builds a working image with multi-stage build, non-root user, and HEALTHCHECK"
    - "K8s Deployment manifest deploys stock-api to ingestion namespace with probes and ConfigMap"
    - "K8s Service manifest exposes stock-api as ClusterIP on port 8000"
    - "deploy-all.sh Phase 3 section is uncommented and references correct manifest filenames"
  artifacts:
    - path: "stock-prediction-platform/services/api/Dockerfile"
      provides: "Multi-stage Docker build for FastAPI service"
      contains: "FROM python:3.11-slim"
    - path: "stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml"
      provides: "K8s Deployment for stock-api in ingestion namespace"
      contains: "kind: Deployment"
    - path: "stock-prediction-platform/k8s/ingestion/fastapi-service.yaml"
      provides: "K8s Service for stock-api ClusterIP"
      contains: "kind: Service"
    - path: "stock-prediction-platform/scripts/deploy-all.sh"
      provides: "Phase 3 section uncommented"
      contains: "kubectl apply -f"
  key_links:
    - from: "stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml"
      to: "stock-prediction-platform/k8s/ingestion/configmap.yaml"
      via: "envFrom configMapRef ingestion-config"
      pattern: "configMapRef"
    - from: "stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml"
      to: "stock-prediction-platform/services/api/Dockerfile"
      via: "image: stock-api:latest"
      pattern: "image: stock-api:latest"
    - from: "stock-prediction-platform/scripts/deploy-all.sh"
      to: "stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml"
      via: "kubectl apply -f"
      pattern: "fastapi-deployment\\.yaml"
---

<objective>
Create the production Dockerfile (multi-stage, non-root), K8s Deployment and Service manifests for the ingestion namespace, and uncomment the Phase 3 section in deploy-all.sh.

Purpose: Containerizes the FastAPI app and makes it deployable to the Kubernetes cluster with health probes, resource limits, and ConfigMap integration.
Output: Buildable Dockerfile, K8s manifests ready for `kubectl apply`, deploy-all.sh updated.
</objective>

<execution_context>
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/workflows/execute-plan.md
@/home/tempa/Desktop/priv-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fastapi-base-service/03-CONTEXT.md
@.planning/phases/03-fastapi-base-service/03-RESEARCH.md
@.planning/phases/03-fastapi-base-service/03-01-SUMMARY.md

@stock-prediction-platform/services/api/requirements.txt
@stock-prediction-platform/k8s/ingestion/configmap.yaml
@stock-prediction-platform/scripts/deploy-all.sh

<interfaces>
<!-- From Plan 01 output — the app entry point the Dockerfile must reference -->
CMD: uvicorn app.main:app --host 0.0.0.0 --port 8000
Health endpoint: GET /health (returns 200 with JSON)

<!-- Existing ConfigMap in ingestion namespace -->
From stock-prediction-platform/k8s/ingestion/configmap.yaml:
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: ingestion-config
  namespace: ingestion
data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka-kafka-bootstrap.storage.svc.cluster.local:9092"
  INTRADAY_TOPIC: "intraday-data"
  HISTORICAL_TOPIC: "historical-data"
  API_BASE_URL: "http://stock-api.ingestion.svc.cluster.local:8000"
  LOG_LEVEL: "INFO"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-stage Dockerfile</name>
  <files>stock-prediction-platform/services/api/Dockerfile</files>
  <read_first>
    stock-prediction-platform/services/api/Dockerfile,
    stock-prediction-platform/services/api/requirements.txt,
    .planning/phases/03-fastapi-base-service/03-CONTEXT.md
  </read_first>
  <action>
Replace the placeholder Dockerfile with a production multi-stage build. Exact content:

**Stage 1 — Builder:**
- `FROM python:3.11-slim AS builder`
- `WORKDIR /app`
- `COPY requirements.txt .`
- `RUN pip install --no-cache-dir -r requirements.txt`

**Stage 2 — Runtime:**
- `FROM python:3.11-slim`
- `RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*` (curl needed for HEALTHCHECK)
- `RUN useradd --create-home --uid 1000 appuser`
- `WORKDIR /app`
- `COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages` (Python packages)
- `COPY --from=builder /usr/local/bin /usr/local/bin` (uvicorn binary — CRITICAL: without this, CMD fails with "command not found")
- `COPY ./app ./app` (only the app package, NOT the whole directory)
- `USER appuser`
- `EXPOSE 8000`
- `HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 CMD curl -f http://localhost:8000/health || exit 1`
- `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`

Do NOT copy requirements.txt or tests/ into the final stage. Only copy `./app`.
  </action>
  <verify>
    <automated>grep -q "FROM python:3.11-slim AS builder" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/Dockerfile && grep -q "HEALTHCHECK" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/Dockerfile && grep -q "USER appuser" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/Dockerfile && grep -q "COPY --from=builder /usr/local/bin" /home/tempa/Desktop/priv-project/stock-prediction-platform/services/api/Dockerfile && echo "Dockerfile OK"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile contains `FROM python:3.11-slim AS builder` (multi-stage)
    - Dockerfile contains `apt-get install -y` with `curl` in the final stage
    - Dockerfile contains `useradd --create-home --uid 1000 appuser` and `USER appuser`
    - Dockerfile copies BOTH `/usr/local/lib/python3.11/site-packages` AND `/usr/local/bin` from builder
    - Dockerfile contains `HEALTHCHECK` with `curl -f http://localhost:8000/health`
    - Dockerfile contains `EXPOSE 8000`
    - CMD is `["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`
    - Only `./app` is copied to final stage (not `.` or `requirements.txt`)
  </acceptance_criteria>
  <done>Multi-stage Dockerfile with builder + runtime stages, non-root user, curl for HEALTHCHECK, uvicorn CMD on port 8000.</done>
</task>

<task type="auto">
  <name>Task 2: Create K8s Deployment and Service manifests, update deploy-all.sh</name>
  <files>
    stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml,
    stock-prediction-platform/k8s/ingestion/fastapi-service.yaml,
    stock-prediction-platform/scripts/deploy-all.sh
  </files>
  <read_first>
    stock-prediction-platform/k8s/ingestion/configmap.yaml,
    stock-prediction-platform/scripts/deploy-all.sh,
    .planning/phases/03-fastapi-base-service/03-CONTEXT.md
  </read_first>
  <action>
**fastapi-deployment.yaml** — Create K8s Deployment manifest:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-api
  namespace: ingestion
  labels:
    app: stock-api
    app.kubernetes.io/part-of: stock-prediction-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-api
  template:
    metadata:
      labels:
        app: stock-api
    spec:
      containers:
        - name: stock-api
          image: stock-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ingestion-config
          env:
            - name: SERVICE_NAME
              value: "stock-api"
            - name: APP_VERSION
              value: "1.0.0"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
```

**fastapi-service.yaml** — Create K8s Service manifest:
```yaml
apiVersion: v1
kind: Service
metadata:
  name: stock-api
  namespace: ingestion
  labels:
    app: stock-api
    app.kubernetes.io/part-of: stock-prediction-platform
spec:
  type: ClusterIP
  selector:
    app: stock-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
```

**deploy-all.sh** — Uncomment lines 30-33 (the Phase 3 section). Change from:
```
# echo "[Phase 3] Deploying FastAPI service..."
# kubectl apply -f "$PROJECT_ROOT/k8s/ingestion/fastapi-deployment.yaml"
# kubectl apply -f "$PROJECT_ROOT/k8s/ingestion/fastapi-service.yaml"
```
To (remove leading `# ` from each line):
```
echo "[Phase 3] Deploying FastAPI service..."
kubectl apply -f "$PROJECT_ROOT/k8s/ingestion/fastapi-deployment.yaml"
kubectl apply -f "$PROJECT_ROOT/k8s/ingestion/fastapi-service.yaml"
```

IMPORTANT: The filenames `fastapi-deployment.yaml` and `fastapi-service.yaml` must match EXACTLY what deploy-all.sh expects. Also add `kubectl apply -f "$PROJECT_ROOT/k8s/ingestion/configmap.yaml"` BEFORE the deployment line if not already present — the ConfigMap must exist before the Deployment references it.
  </action>
  <verify>
    <automated>grep -q "kind: Deployment" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml && grep -q "namespace: ingestion" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml && grep -q "kind: Service" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/ingestion/fastapi-service.yaml && grep -q "configMapRef" /home/tempa/Desktop/priv-project/stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml && grep -v "^#" /home/tempa/Desktop/priv-project/stock-prediction-platform/scripts/deploy-all.sh | grep -q "fastapi-deployment.yaml" && echo "K8s manifests OK"</automated>
  </verify>
  <acceptance_criteria>
    - `fastapi-deployment.yaml` exists in `k8s/ingestion/` with `kind: Deployment`, `namespace: ingestion`, `image: stock-api:latest`, `imagePullPolicy: Never`
    - Deployment has `envFrom` with `configMapRef: name: ingestion-config`
    - Deployment has explicit `env` entries for `SERVICE_NAME` and `APP_VERSION`
    - Deployment has `livenessProbe` and `readinessProbe` both pointing to `/health` port `8000`
    - Deployment has resource requests `250m`/`512Mi` and limits `1`/`1Gi`
    - `fastapi-service.yaml` exists with `kind: Service`, `type: ClusterIP`, port `8000` -> targetPort `8000`
    - `deploy-all.sh` Phase 3 lines are uncommented (no leading `#`)
    - `bash -n deploy-all.sh` passes (valid syntax)
  </acceptance_criteria>
  <done>K8s Deployment (with probes, ConfigMap, resources) and Service (ClusterIP:8000) created. deploy-all.sh Phase 3 section uncommented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Docker build and K8s deployment</name>
  <files>stock-prediction-platform/services/api/Dockerfile</files>
  <action>
Human verifies the full deployment flow. Claude should attempt the Docker build automatically before presenting the checkpoint:
1. `cd stock-prediction-platform/services/api && docker build -t stock-api:latest .`
2. If build succeeds, run a quick smoke test: `docker run --rm -d -p 8000:8000 --name stock-api-test stock-api:latest` then `curl -sf http://localhost:8000/health` then `docker stop stock-api-test`
3. Present results to user for K8s deployment verification.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification of Docker build and K8s deployment"</automated>
  </verify>
  <done>User confirms Docker image builds, container starts, /health returns 200, and (if cluster running) K8s pod reaches Running state.</done>
</task>

</tasks>

<verification>
1. Dockerfile syntax: `grep -c "FROM" stock-prediction-platform/services/api/Dockerfile` should return 2 (multi-stage)
2. K8s manifests valid: `kubectl apply --dry-run=client -f stock-prediction-platform/k8s/ingestion/fastapi-deployment.yaml` (if kubectl available)
3. deploy-all.sh syntax: `bash -n stock-prediction-platform/scripts/deploy-all.sh`
4. Full flow: Docker build -> run -> curl /health -> get 200 with correct JSON
</verification>

<success_criteria>
- Dockerfile: Multi-stage (python:3.11-slim), curl installed, non-root appuser, HEALTHCHECK, copies site-packages + /usr/local/bin
- fastapi-deployment.yaml: ingestion namespace, 1 replica, envFrom ingestion-config, SERVICE_NAME/APP_VERSION env, both probes, resource limits
- fastapi-service.yaml: ClusterIP, port 8000
- deploy-all.sh: Phase 3 lines uncommented
- Docker build succeeds and container responds to /health with correct JSON
</success_criteria>

<output>
After completion, create `.planning/phases/03-fastapi-base-service/03-02-SUMMARY.md`
</output>
