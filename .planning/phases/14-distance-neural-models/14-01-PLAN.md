# Plan 14-01: Distance, SVM & Neural Model Configs + Search Spaces

**Phase:** 14 — Distance, SVM & Neural Models
**Scope:** MODEL-13, MODEL-14, MODEL-15, MODEL-21 (search spaces)
**Files:** `ml/models/model_configs.py`, `ml/models/__init__.py`
**Tests:** `ml/tests/test_model_configs.py` (extend)

---

## Steps

### 1. Add `DISTANCE_NEURAL_MODELS` dict to `model_configs.py`

Three sklearn entries with search spaces:

- `knn`: KNeighborsRegressor
  - default_params: `{}`
  - search_space: `n_neighbors` [3,5,7,9,11,15,21], `weights` ["uniform","distance"], `metric` ["euclidean","manhattan"]
  - n_iter: 30

- `svr`: SVR
  - default_params: `{"kernel": "rbf"}`
  - search_space: `C` np.logspace(-2, 2, 10), `gamma` ["scale","auto",0.001,0.01,0.1], `epsilon` np.linspace(0.01, 0.5, 10)
  - n_iter: 30

- `mlp`: MLPRegressor
  - default_params: `{"random_state": 42, "early_stopping": True, "n_iter_no_change": 10, "max_iter": 500}`
  - search_space: `hidden_layer_sizes` [(64,),(128,),(64,32),(128,64)], `alpha` np.logspace(-5,-1,10), `learning_rate_init` [0.001,0.005,0.01]
  - n_iter: 30

Add new imports at top of file:
```python
from sklearn.neighbors import KNeighborsRegressor
from sklearn.svm import SVR
from sklearn.neural_network import MLPRegressor
```

### 2. Register the new family at module level

After `DISTANCE_NEURAL_MODELS` dict definition:
```python
_MODEL_FAMILIES["distance_neural"] = DISTANCE_NEURAL_MODELS
```

### 3. Update `ml/models/__init__.py` exports

Add `DISTANCE_NEURAL_MODELS` to imports and `__all__`.

### 4. Extend `ml/tests/test_model_configs.py`

New test class `TestDistanceNeuralModels`:
- `test_three_models_present` — len == 3
- `test_expected_names` — {"knn", "svr", "mlp"}
- `test_model_class_is_sklearn` (parametrize) — all have fit/predict
- `test_knn_search_space` — n_neighbors, weights, metric present
- `test_svr_search_space` — C, gamma, epsilon present
- `test_svr_default_kernel` — default_params["kernel"] == "rbf"
- `test_mlp_search_space` — hidden_layer_sizes, alpha, learning_rate_init present
- `test_mlp_early_stopping` — default_params["early_stopping"] is True
- `test_mlp_max_iter_bounded` — default_params["max_iter"] <= 500

Extend existing `TestGetModelConfigs`:
- `test_get_distance_neural` — get_model_configs("distance_neural") returns 3
- `test_get_all_includes_distance_neural` — get_all_model_configs() includes knn, svr, mlp

---

## Verification
- `python -m pytest ml/tests/test_model_configs.py -v`
