---
phase: 36-secrets-management-db-rbac
plan: 02
type: execute
wave: 1
depends_on: [36-01]
files_modified:
  - db/migrations/002_rbac_roles.sql
  - k8s/storage/secrets.yaml
  - k8s/storage/secrets.yaml.example
  - scripts/deploy-all.sh
  - README.md
autonomous: true
must_haves:
  truths:
    # --- db/migrations/002_rbac_roles.sql ---
    - SQL migration creates role stock_readonly with LOGIN and a password (parameterized or placeholder)
    - SQL migration creates role stock_writer with LOGIN and a password (parameterized or placeholder)
    - stock_readonly gets CONNECT ON DATABASE stockdb
    - stock_readonly gets USAGE ON SCHEMA public
    - stock_readonly gets SELECT ON ALL TABLES IN SCHEMA public
    - stock_readonly gets ALTER DEFAULT PRIVILEGES for SELECT on future tables
    - stock_writer gets CONNECT ON DATABASE stockdb
    - stock_writer gets USAGE ON SCHEMA public
    - stock_writer gets SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public
    - stock_writer gets USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public (for SERIAL/BIGSERIAL inserts)
    - stock_writer gets ALTER DEFAULT PRIVILEGES for full DML on future tables and sequences
    - stock_readonly does NOT get any INSERT, UPDATE, DELETE, CREATE, DROP, or ALTER permissions
    - stock_writer does NOT get any CREATE, DROP, ALTER, or TRUNCATE permissions
    - Migration uses IF NOT EXISTS or DO $$ block to be idempotent (re-runnable without error)
    - Migration has a header comment explaining the role mapping (which service uses which role)
    # --- secrets.yaml update ---
    - secrets.yaml adds DATABASE_URL_READONLY key with base64-encoded postgresql://stock_readonly:<password>@host/stockdb
    - secrets.yaml adds DATABASE_URL_WRITER key with base64-encoded postgresql://stock_writer:<password>@host/stockdb
    - secrets.yaml retains DATABASE_URL key as admin connection for backward compat (alembic)
    - secrets.yaml.example updated to match new keys with CHANGEME placeholders
    # --- Per-service DATABASE_URL differentiation ---
    - k8s/ingestion/fastapi-deployment.yaml uses DATABASE_URL_READONLY from the Secret (stock_readonly role)
    - k8s/ml/model-serving.yaml uses DATABASE_URL_READONLY from the Secret (stock_readonly role)
    - k8s/processing/kafka-consumer-deployment.yaml uses DATABASE_URL_WRITER from the Secret (stock_writer role)
    - k8s/ml/cronjob-training.yaml uses DATABASE_URL_WRITER from the Secret (stock_writer role)
    - k8s/ml/cronjob-drift.yaml uses DATABASE_URL_WRITER from the Secret (stock_writer role)
    - stock-api entrypoint.sh uses DATABASE_URL (admin) for alembic; app uses DATABASE_URL_READONLY at runtime
    # --- deploy-all.sh update ---
    - deploy-all.sh runs the RBAC migration SQL after PostgreSQL is ready
    - Uses kubectl exec to run psql -f on the SQL file, or kubectl cp + exec pattern
    # --- README update ---
    - README.md adds a "Secrets Management" section documenting the workflow
    - Documents how to create secrets.yaml from the example template
    - Documents the Sealed Secrets workflow for production
    - Documents the three DB roles and which services use each
  artifacts:
    - db/migrations/002_rbac_roles.sql (NEW)
    - k8s/storage/secrets.yaml (modified — new keys)
    - k8s/storage/secrets.yaml.example (modified — new keys)
    - k8s/ingestion/fastapi-deployment.yaml (modified — per-role DATABASE_URL)
    - k8s/processing/kafka-consumer-deployment.yaml (modified — per-role DATABASE_URL)
    - k8s/ml/model-serving.yaml (modified — per-role DATABASE_URL)
    - k8s/ml/cronjob-training.yaml (modified — per-role DATABASE_URL)
    - k8s/ml/cronjob-drift.yaml (modified — per-role DATABASE_URL)
    - scripts/deploy-all.sh (modified — RBAC migration step)
    - README.md (modified — secrets + RBAC docs)
  key_links:
    - db/init.sql (existing schema — roles grant access to these tables)
    - k8s/storage/secrets.yaml (updated in plan 01)
    - scripts/deploy-all.sh (updated in plan 01 — extended here)
    - services/api/entrypoint.sh (Phase 35 — uses DATABASE_URL for alembic)
    - services/api/app/config.py (loads DATABASE_URL from env)
    - .planning/phases/36/36-CONTEXT.md (architecture and role mapping)
---

<objective>
Implement database RBAC roles (stock_readonly, stock_writer) and configure per-service DATABASE_URL mapping so each service connects with least-privilege credentials.

Purpose: DBHARD-07 (Database RBAC roles: stock_readonly for API reads, stock_writer for consumer + ML writes).
Output: SQL migration creating roles+grants, updated K8s manifests with per-role connection strings, deployment script integration.
</objective>

<execution_context>
@~/.Copilot/skills/execute-plan/SKILL.md
@~/.Copilot/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36/36-CONTEXT.md

Key references:
@db/init.sql (schema — roles grant access to these tables)
@k8s/storage/secrets.yaml (created in plan 01 — extended here)
@k8s/storage/secrets.yaml.example (created in plan 01 — extended here)
@k8s/ingestion/fastapi-deployment.yaml (updated in plan 01 — refined here)
@k8s/processing/kafka-consumer-deployment.yaml (uses secretRef — add per-role env)
@k8s/ml/model-serving.yaml (uses secretRef — add per-role env)
@k8s/ml/cronjob-training.yaml (updated in plan 01 — refined here)
@k8s/ml/cronjob-drift.yaml (updated in plan 01 — refined here)
@scripts/deploy-all.sh (updated in plan 01 — add RBAC step)
@services/api/app/config.py (loads DATABASE_URL from env)
@services/api/entrypoint.sh (runs alembic with DATABASE_URL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create db/migrations/002_rbac_roles.sql</name>
  <files>db/migrations/002_rbac_roles.sql</files>
  <action>

Create the RBAC migration at `db/migrations/002_rbac_roles.sql`:

```sql
-- 002_rbac_roles.sql — Database RBAC for stock-prediction-platform
--
-- Role mapping:
--   stockuser      → admin (DDL, schema migrations via Alembic)
--   stock_readonly  → SELECT only (API reads, model serving)
--   stock_writer    → DML (Kafka consumer upserts, ML predictions, drift logs)
--
-- Passwords are set from environment variables during deploy.
-- This script uses placeholder passwords; deploy-all.sh overrides them.
--
-- Idempotent: safe to re-run.

BEGIN;

-- ============================================================
-- 1. Create roles (idempotent via DO block)
-- ============================================================
DO $$
BEGIN
    IF NOT EXISTS (SELECT FROM pg_catalog.pg_roles WHERE rolname = 'stock_readonly') THEN
        CREATE ROLE stock_readonly LOGIN PASSWORD 'readonly_changeme';
    END IF;

    IF NOT EXISTS (SELECT FROM pg_catalog.pg_roles WHERE rolname = 'stock_writer') THEN
        CREATE ROLE stock_writer LOGIN PASSWORD 'writer_changeme';
    END IF;
END
$$;

-- ============================================================
-- 2. stock_readonly — SELECT only
-- ============================================================
GRANT CONNECT ON DATABASE stockdb TO stock_readonly;
GRANT USAGE ON SCHEMA public TO stock_readonly;
GRANT SELECT ON ALL TABLES IN SCHEMA public TO stock_readonly;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO stock_readonly;

-- ============================================================
-- 3. stock_writer — full DML (no DDL)
-- ============================================================
GRANT CONNECT ON DATABASE stockdb TO stock_writer;
GRANT USAGE ON SCHEMA public TO stock_writer;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO stock_writer;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO stock_writer;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
    GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO stock_writer;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
    GRANT USAGE, SELECT ON SEQUENCES TO stock_writer;

COMMIT;
```

  </action>
</task>

<task type="auto">
  <name>Task 2: Add per-role DATABASE_URL keys to secrets.yaml</name>
  <files>k8s/storage/secrets.yaml, k8s/storage/secrets.yaml.example</files>
  <action>

Update `k8s/storage/secrets.yaml` to add role-specific connection strings:

```yaml
data:
  # Admin (Alembic migrations)
  # echo -n 'devpassword123' | base64
  POSTGRES_PASSWORD: ZGV2cGFzc3dvcmQxMjM=
  # echo -n 'postgresql://stockuser:devpassword123@postgresql.storage.svc.cluster.local:5432/stockdb' | base64
  DATABASE_URL: cG9zdGdyZXNxbDovL3N0b2NrdXNlcjpkZXZwYXNzd29yZDEyM0Bwb3N0Z3Jlc3FsLnN0b3JhZ2Uuc3ZjLmNsdXN0ZXIubG9jYWw6NTQzMi9zdG9ja2Ri
  # Read-only (API, model-serving)
  # echo -n 'postgresql://stock_readonly:readonly_changeme@postgresql.storage.svc.cluster.local:5432/stockdb' | base64
  DATABASE_URL_READONLY: cG9zdGdyZXNxbDovL3N0b2NrX3JlYWRvbmx5OnJlYWRvbmx5X2NoYW5nZW1lQHBvc3RncmVzcWwuc3RvcmFnZS5zdmMuY2x1c3Rlci5sb2NhbDo1NDMyL3N0b2NrZGI=
  # Writer (kafka-consumer, ML training, drift)
  # echo -n 'postgresql://stock_writer:writer_changeme@postgresql.storage.svc.cluster.local:5432/stockdb' | base64
  DATABASE_URL_WRITER: cG9zdGdyZXNxbDovL3N0b2NrX3dyaXRlcjp3cml0ZXJfY2hhbmdlbWVAcG9zdGdyZXNxbC5zdG9yYWdlLnN2Yy5jbHVzdGVyLmxvY2FsOjU0MzIvc3RvY2tkYg==
```

Update `k8s/storage/secrets.yaml.example` with matching keys using CHANGEME placeholders.

  </action>
</task>

<task type="auto">
  <name>Task 3: Wire per-role DATABASE_URL in K8s deployments</name>
  <files>k8s/ingestion/fastapi-deployment.yaml, k8s/processing/kafka-consumer-deployment.yaml, k8s/ml/model-serving.yaml, k8s/ml/cronjob-training.yaml, k8s/ml/cronjob-drift.yaml</files>
  <action>

Each service needs to receive the correct DATABASE_URL for its role. Rather than using `secretRef` (which injects ALL keys), use explicit `env` entries with `secretKeyRef` to map the role-specific key to the standard `DATABASE_URL` env var that the application expects.

**k8s/ingestion/fastapi-deployment.yaml** — API uses readonly + admin for migrations:
Replace the generic secretRef added in plan 01 with explicit env vars:
```yaml
          env:
            - name: SERVICE_NAME
              value: "stock-api"
            - name: APP_VERSION
              value: "1.0.0"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: stock-platform-secrets
                  key: DATABASE_URL_READONLY
            - name: DATABASE_URL_ADMIN
              valueFrom:
                secretKeyRef:
                  name: stock-platform-secrets
                  key: DATABASE_URL
```
NOTE: The entrypoint.sh should use DATABASE_URL_ADMIN for alembic migrations if set, falling back to DATABASE_URL. Update entrypoint.sh to check `DATABASE_URL_ADMIN` first: `ALEMBIC_DB_URL="${DATABASE_URL_ADMIN:-$DATABASE_URL}"`.

**k8s/processing/kafka-consumer-deployment.yaml** — Consumer uses writer:
Replace `secretRef` with explicit env:
```yaml
          envFrom:
            - configMapRef:
                name: processing-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: stock-platform-secrets
                  key: DATABASE_URL_WRITER
```

**k8s/ml/model-serving.yaml** — Model serving uses readonly:
Replace `secretRef` with explicit env:
```yaml
          envFrom: []
          env:
            - name: MODEL_REGISTRY_PATH
              value: "/models"
            - name: LOG_LEVEL
              value: "INFO"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: stock-platform-secrets
                  key: DATABASE_URL_READONLY
```

**k8s/ml/cronjob-training.yaml** — Training uses writer:
Replace `secretRef` with explicit env addition:
```yaml
              envFrom:
                - configMapRef:
                    name: ml-pipeline-config
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: stock-platform-secrets
                      key: DATABASE_URL_WRITER
```

**k8s/ml/cronjob-drift.yaml** — Drift uses writer:
Same pattern as training:
```yaml
              envFrom:
                - configMapRef:
                    name: ml-pipeline-config
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: stock-platform-secrets
                      key: DATABASE_URL_WRITER
```

  </action>
</task>

<task type="auto">
  <name>Task 4: Update entrypoint.sh for admin migration URL</name>
  <files>services/api/entrypoint.sh</files>
  <action>

Update `services/api/entrypoint.sh` to use the admin DATABASE_URL for Alembic migrations while the app runs with the readonly URL:

```bash
#!/bin/bash
set -e

# Use admin URL for migrations (DDL requires full privileges)
ALEMBIC_DB_URL="${DATABASE_URL_ADMIN:-$DATABASE_URL}"

if [ -n "$ALEMBIC_DB_URL" ]; then
    echo "[entrypoint] Running database migrations..."
    DATABASE_URL="$ALEMBIC_DB_URL" alembic upgrade head
    echo "[entrypoint] Migrations complete."
else
    echo "[entrypoint] DATABASE_URL not set — skipping migrations."
fi

# Start the API server (DATABASE_URL is the readonly connection)
echo "[entrypoint] Starting uvicorn..."
exec uvicorn app.main:app --host 0.0.0.0 --port 8000
```

This ensures Alembic runs with admin (DDL) privileges while the running app uses stock_readonly.

  </action>
</task>

<task type="auto">
  <name>Task 5: Add RBAC migration step to deploy-all.sh</name>
  <files>scripts/deploy-all.sh</files>
  <action>

Add the RBAC migration step AFTER the PostgreSQL pod is ready (after applying postgresql-deployment.yaml). Insert after the "wait for postgresql" step:

```bash
echo "--- Applying Database RBAC Roles ---"
echo "Waiting for PostgreSQL to be ready..."
kubectl wait --for=condition=ready pod -l app=postgresql -n storage --timeout=120s

echo "Applying RBAC roles migration..."
kubectl cp db/migrations/002_rbac_roles.sql storage/$(kubectl get pod -l app=postgresql -n storage -o jsonpath='{.items[0].metadata.name}'):/tmp/002_rbac_roles.sql
kubectl exec -n storage $(kubectl get pod -l app=postgresql -n storage -o jsonpath='{.items[0].metadata.name}') -- \
    psql -U stockuser -d stockdb -f /tmp/002_rbac_roles.sql

# Update role passwords from secrets
READONLY_PWD=$(kubectl get secret stock-platform-secrets -n storage -o jsonpath='{.data.POSTGRES_READONLY_PASSWORD}' | base64 -d)
WRITER_PWD=$(kubectl get secret stock-platform-secrets -n storage -o jsonpath='{.data.POSTGRES_WRITER_PASSWORD}' | base64 -d)

if [ -n "$READONLY_PWD" ]; then
    kubectl exec -n storage $(kubectl get pod -l app=postgresql -n storage -o jsonpath='{.items[0].metadata.name}') -- \
        psql -U stockuser -d stockdb -c "ALTER ROLE stock_readonly PASSWORD '$READONLY_PWD';"
fi
if [ -n "$WRITER_PWD" ]; then
    kubectl exec -n storage $(kubectl get pod -l app=postgresql -n storage -o jsonpath='{.items[0].metadata.name}') -- \
        psql -U stockuser -d stockdb -c "ALTER ROLE stock_writer PASSWORD '$WRITER_PWD';"
fi

echo "RBAC roles applied."
```

  </action>
</task>

<task type="auto">
  <name>Task 6: Add Secrets Management section to README.md</name>
  <files>README.md</files>
  <action>

Add a "Secrets Management" section to the README after the existing deployment/setup sections:

```markdown
## Secrets Management

### K8s Secrets Setup

1. Copy the template: `cp k8s/storage/secrets.yaml.example k8s/storage/secrets.yaml`
2. Generate passwords and encode to base64: `echo -n 'your-secure-password' | base64`
3. Replace placeholder values in `secrets.yaml` with real base64-encoded credentials
4. Run `scripts/deploy-all.sh` — secrets are applied automatically to all namespaces

**Never commit `secrets.yaml`** — it is listed in `.gitignore`.

### Database Roles

| Role | Access Level | Used By |
|------|-------------|---------|
| `stockuser` | Admin (DDL + DML) | Alembic migrations |
| `stock_readonly` | SELECT only | API service, model serving |
| `stock_writer` | SELECT + INSERT + UPDATE + DELETE | Kafka consumer, ML training, drift detection |

### Production: Sealed Secrets

For production clusters, use [Sealed Secrets](https://github.com/bitnami-labs/sealed-secrets):

```bash
# Install kubeseal CLI
# Seal the secret (encrypted, safe to commit)
kubeseal --format yaml < k8s/storage/secrets.yaml > k8s/storage/sealed-secrets.yaml
kubectl apply -f k8s/storage/sealed-secrets.yaml
```
```

  </action>
</task>

</tasks>
