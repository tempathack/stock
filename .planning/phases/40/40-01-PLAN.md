---
phase: 40-sqlalchemy-connection-pooling
plan: 01
type: execute
wave: 1
depends_on: [39-02]
files_created:
  - services/api/app/db.py
files_modified:
  - services/api/app/main.py
  - services/api/app/config.py
  - services/api/app/routers/health.py
  - services/api/app/models/database.py
autonomous: true
scope_exclusions:
  - Kafka consumer connection pooling (uses psycopg2.pool.SimpleConnectionPool — adequate for that service)
  - ML pipeline DB access (runs as batch jobs, not long-lived — raw psycopg2 is acceptable)
  - Alembic migration runner (already uses engine_from_config with NullPool — UNCHANGED)
  - Full ORM-based query rewrite of services (deferred to Plan 02)
must_haves:
  truths:
    # --- services/api/app/db.py (NEW) ---
    - db.py creates an async engine via create_async_engine() with driver "postgresql+asyncpg://"
    - db.py reads DATABASE_URL from app.config.settings
    - db.py normalizes DATABASE_URL: replaces "postgresql://" with "postgresql+asyncpg://" and "psycopg2://" with "asyncpg://"
    - Engine pool_size is settings.DB_POOL_SIZE (default 10)
    - Engine max_overflow is settings.DB_MAX_OVERFLOW (default 20)
    - Engine pool_pre_ping is True (validates connections before use)
    - Engine pool_recycle is 3600 (recycle connections after 1 hour)
    - Engine echo is False in production (controlled by settings.DEBUG)
    - db.py defines async_session_factory as async_sessionmaker bound to the engine
    - async_session_factory has expire_on_commit=False (prevents lazy-load issues after commit)
    - db.py exports an async generator get_async_session() that yields an AsyncSession
    - get_async_session() can be used as a FastAPI Depends() dependency
    - get_async_session() properly closes the session in a finally block
    - db.py exports an async function init_db() that verifies connectivity by executing "SELECT 1"
    - db.py exports an async function close_db() that calls engine.dispose()
    - db.py exports a function get_pool_status() that returns dict with pool.size(), pool.checkedin(), pool.checkedout(), pool.overflow()
    - get_pool_status() returns None when engine is None or not initialized
    # --- services/api/app/config.py (MODIFIED — MINIMAL) ---
    - DB_POOL_SIZE and DB_MAX_OVERFLOW already exist (value 10 and 20) — UNCHANGED
    - Adds DB_POOL_PRE_PING: bool = True
    - Adds DB_POOL_RECYCLE: int = 3600
    - All other settings UNCHANGED
    # --- services/api/app/main.py (MODIFIED) ---
    - Imports init_db and close_db from app.db
    - lifespan startup section calls await init_db() AFTER configure_uvicorn_logging()
    - lifespan shutdown section (after yield) calls await close_db()
    - init_db() failure logs error but does NOT crash the app (service can still serve health + fallbacks)
    - Existing middleware, router includes, and Instrumentator are UNCHANGED
    # --- services/api/app/routers/health.py (MODIFIED) ---
    - HealthResponse model gains optional field: db_pool (dict | None)
    - health_check() imports get_pool_status from app.db
    - health_check() calls get_pool_status() and includes result in response
    - db_pool dict contains: pool_size, checked_in, checked_out, overflow, status ("connected" | "unavailable")
    - When pool is unavailable, db_pool is None and overall status remains "ok" (degraded operation)
    - Existing service/version/status fields UNCHANGED
    # --- services/api/app/models/database.py (MODIFIED — BACKWARDS COMPAT) ---
    - get_db_connection() is NOT removed (Plan 02 migrates callers first)
    - A deprecation warning is added to get_db_connection() docstring
    - database.py gains a new async context manager: async_db_connection() that yields an AsyncSession from db.py
    - async_db_connection() is available for incremental migration in Plan 02
    - No other changes to database.py
  artifacts:
    - services/api/app/db.py (NEW — async engine, session factory, pool status)
    - services/api/app/config.py (modified — 2 new pool settings)
    - services/api/app/main.py (modified — init_db/close_db in lifespan)
    - services/api/app/routers/health.py (modified — pool metrics in response)
    - services/api/app/models/database.py (modified — deprecation note + async helper)
  key_links:
    - services/api/requirements.txt (sqlalchemy==2.0.30, asyncpg==0.29.0 already present — UNCHANGED)
    - services/api/alembic/env.py (sync migration runner — UNCHANGED)
    - services/api/app/models/orm.py (ORM models — UNCHANGED, used by db.py for metadata only)
---

<objective>
Create the SQLAlchemy async engine with connection pooling (pool_size=10, max_overflow=20) as
a new `services/api/app/db.py` module. Wire engine lifecycle into FastAPI lifespan and expose
pool metrics via the /health endpoint.

Purpose: DBHARD-04 (SQLAlchemy async engine with connection pooling).
Output: Async engine initializes on app startup, pool stats are visible at /health, and
`get_async_session()` dependency is ready for Plan 02 migration.
</objective>

<scope_exclusions>
- Service function migration to async sessions (Plan 02 scope)
- Kafka consumer pooling changes (SimpleConnectionPool is adequate for consumer workload)
- ML pipeline DB changes (batch job, not a long-lived server)
- Alembic env.py changes (sync runner — already correct)
</scope_exclusions>

<execution_context>
@~/.Copilot/skills/execute-plan/SKILL.md
@~/.Copilot/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key references:
@services/api/app/config.py (settings — DB_POOL_SIZE, DB_MAX_OVERFLOW already defined)
@services/api/app/main.py (lifespan — add init_db/close_db)
@services/api/app/models/orm.py (Base — ORM models for metadata reference)
@services/api/app/models/database.py (get_db_connection — add deprecation + async helper)
@services/api/app/routers/health.py (health_check — add pool metrics)
@services/api/requirements.txt (sqlalchemy==2.0.30, asyncpg==0.29.0 — both already present)
@services/api/alembic/env.py (sync migration runner — UNCHANGED reference)
</context>
