# Phase 91 — Plan 02: Router cached-file fallback (predict.py)

## Goal
When the horizon-specific cached JSON file is absent, the router scales the base
`latest.json` file rather than letting the request fall through to the DB.

## Tasks

- [x] Import `_apply_horizon_scaling` in `predict.py` router
  - File: `services/api/app/routers/predict.py`

- [x] Add scaling fallback in `predict_bulk`
  - After `load_cached_predictions(horizon=horizon)` returns empty:
    - Try `load_cached_predictions(horizon=None)` → base file
    - If base file exists, call `_apply_horizon_scaling(base_raw, horizon)`
  - File: `services/api/app/routers/predict.py`

## Verification
```
pytest services/api/tests/ -k predict -v
```
