# Plan 27-03 — SHAP Panel, Responsive Polish & Build Verification

## Objective

Add the per-stock SHAP explanation panel, polish responsive layout for all forecast components, handle edge cases (no predictions, API down, empty filters), and run the final build verification. Delivers FFOR-06 + production polish.

## Dependencies

- Plan 27-01 complete — ForecastTable, ForecastFilters, types, mock data ✅
- Plan 27-02 complete — StockComparisonPanel, StockDetailChart, IndicatorOverlayCharts ✅
- `ShapBarChart` component from Phase 26 — reusable ✅
- `generateMockShapImportance` from `src/utils/mockModelData.ts` ✅
- Full build passes from Plan 27-02 ✅

## Files to Create / Update

<tasks>

<task type="auto">
  <name>Task 1: StockShapPanel component (FFOR-06)</name>
  <context>A SHAP explanation panel tailored for per-stock context. Reuses the ShapBarChart from Phase 26 but adds stock-specific framing: which features drove this specific prediction. Uses mock data seeded by ticker name.</context>

  <files>
    <file path="services/frontend/src/components/forecasts/StockShapPanel.tsx" action="create">StockShapPanel component:

      **Props:**
      ```typescript
      {
        ticker: string;
        modelName: string;
      }
      ```

      **Renders:**
      - Card wrapper: `bg-bg-surface rounded-lg border border-border p-4`
      - Header: "Feature Contribution — {ticker}" with subtitle "Model: {modelName}"
      - Uses `generateMockShapImportance(ticker)` from `@/utils/mockModelData` to get feature importance data
        - In future: replaced by a dedicated SHAP-per-ticker API endpoint
      - Renders `ShapBarChart` from `@/components/charts` with the importance data
        - Custom title: "SHAP Feature Importance for {ticker}"
      - Below the chart: explanatory text:
        - "Features driving the 7-day prediction. Higher bars indicate stronger influence on the forecast."
      - If no data: "SHAP analysis not available for this stock"
    </file>
  </files>

  <verify>
    <cmd>cd services/frontend &amp;&amp; npx tsc --noEmit</cmd>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire StockShapPanel into detail section</name>
  <context>Add the SHAP panel to the stock detail section in Forecasts.tsx, below the indicator charts.</context>

  <files>
    <file path="services/frontend/src/components/forecasts/index.ts" action="update">Add export:
      ```typescript
      export { default as StockShapPanel } from "./StockShapPanel";
      ```
    </file>

    <file path="services/frontend/src/pages/Forecasts.tsx" action="update">In the detail section (after IndicatorOverlayCharts):
      - Import `StockShapPanel` from `@/components/forecasts`
      - Add:
        ```tsx
        <StockShapPanel
          ticker={selectedTicker}
          modelName={forecastRow?.model_name ?? "Unknown"}
        />
        ```
    </file>
  </files>

  <verify>
    <cmd>cd services/frontend &amp;&amp; npx tsc --noEmit</cmd>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Responsive layout polish</name>
  <context>Ensure all forecast components are mobile-friendly and look good at all breakpoints. Apply consistent spacing, text sizing, and overflow handling.</context>

  <files>
    <file path="services/frontend/src/pages/Forecasts.tsx" action="update">Responsive refinements:
      - Detail section: `grid grid-cols-1 xl:grid-cols-2 gap-6` for chart + SHAP side by side on wide screens
      - On narrow screens: stack vertically with `space-y-6`
      - Comparison panel: horizontal scroll on mobile, grid on desktop
      - Filter bar: `flex flex-wrap` on mobile (filters stack), `flex-nowrap` on desktop
      - Table: `overflow-x-auto` with sticky first column (ticker) on mobile
    </file>

    <file path="services/frontend/src/components/forecasts/ForecastTable.tsx" action="update">Responsive table:
      - Wrap in `overflow-x-auto`
      - Ticker column: `sticky left-0 bg-bg-surface z-10` on mobile
      - Reduce padding on mobile: `px-2 py-1 text-sm` vs `px-4 py-3` on desktop
      - Hide less important columns (Company, Sector) on small screens via `hidden sm:table-cell`
    </file>

    <file path="services/frontend/src/components/forecasts/ForecastFilters.tsx" action="update">Responsive filters:
      - `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3` for filter inputs
      - Clear button: full width on mobile, auto on desktop
    </file>
  </files>

  <verify>
    <manual>Verify layout at 320px, 768px, 1024px, 1440px widths. Table scrolls horizontally on mobile. Filters stack cleanly.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Empty states and edge case handling</name>
  <context>Handle: no predictions available, API errors, empty filter results, no indicator data for a ticker. Ensure graceful degradation.</context>

  <files>
    <file path="services/frontend/src/pages/Forecasts.tsx" action="update">Edge case handling:
      - **No predictions + no market data (both APIs failed):** Show ErrorFallback with retry
      - **No predictions but market data available:** Show market data in table with "—" for prediction columns, subtle banner "Predictions not yet generated"
      - **Empty filter results:** Table shows "No forecasts match the current filters" with a "Clear filters" link
      - **Selected ticker has no indicator data:** Detail chart shows "No historical data available for {ticker}" placeholder
      - **Comparison with < 2 tickers:** Panel shows hint message (already in StockComparisonPanel logic)
    </file>
  </files>

  <verify>
    <manual>Test each edge case scenario by temporarily disabling data sources / setting empty data.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 5: Final build verification</name>
  <context>Run TypeScript check and production build to confirm zero errors. Verify all exports are correct.</context>

  <files>
    <file action="no-change">No file changes — verification only</file>
  </files>

  <verify>
    <cmd>cd services/frontend &amp;&amp; npx tsc --noEmit</cmd>
    <cmd>cd services/frontend &amp;&amp; npm run build</cmd>
    <manual>Verify: Build completes with 0 errors. All forecast components tree-shaken correctly. No unused imports.</manual>
  </verify>
</task>

</tasks>
